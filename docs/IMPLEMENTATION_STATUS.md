# Implementation Status

> **What this is:** A live rollup of *build state* — what exists in code today, on which subsystem branch, and what is blocking it. One row per subsystem, not per task.
>
> **What this is not:** Not architecture (see `ADR_INDEX.md` → `THREAT_ENGINE_SPEC.md` → `EVENT_CONTRACTS.md` → `DATABASE_SCHEMA.md` → `DEEPSTREAM_PIPELINE_SPEC.md` → `FRONTEND_BACKEND_CONTRACTS.md` → `AGENTS.md`, per `CLAUDE.md`'s priority order). Not the milestone sequence — that is `docs/IMPLEMENTATION_ROADMAP.md`, the single source of truth for what each `RM-XX` covers and in what order. Not a ticket backlog — that is `TASKS.md` (root), which owns individual tickets, acceptance criteria, and blockers (format currently under review). This file only tracks the subsystem-level *result* of that work: is the code there, and on which branch.
>
> **When to update:** Any time a subsystem's status, branch, or blocker changes — in the same PR/commit that causes the change, not on a schedule. Stale rows are worse than missing rows.
>
> **Who updates it:** Whoever changes the state — human or AI assistant.
>
> **Recommended read order when starting work in this repo:**
> `CLAUDE.md` → `PROJECT_CONTEXT.md` → `ADR_INDEX.md` → `TASKS.md` (root) → `docs/IMPLEMENTATION_ROADMAP.md` → **this file** → subsystem documentation / code.
> Read `docs/IMPLEMENTATION_ROADMAP.md` to know what a milestone means and its dependencies. Read this file to see how far the relevant subsystem branch has actually progressed, without re-deriving that from `git log` or the source tree. Then go to the architecture docs (priority order above) for how a subsystem must be built. Do not infer status from this file alone when the task is destructive or high-stakes — verify against the actual source tree first, since this file can lag reality.

---

## Architecture Contract

- This document tracks **implementation state only** — what is built, in progress, or blocked.
- It is **not** an architecture document and carries no authority over design decisions.
- It must **never** be edited to reflect, justify, or work around a change to architecture — architecture changes belong in an ADR (`ADR_INDEX.md`) per `CLAUDE.md`'s priority order.
- If implementation work conflicts with an architecture document, **implementation stops**. Do not resolve the conflict by editing the architecture doc or by proceeding around it — raise it for clarification (new ADR or human decision) first.

## Source of Truth

This document reflects the **operational implementation state** as last recorded by whoever updated it — it is a report, not a ledger. If it temporarily disagrees with the actual source code (a merged PR that hasn't updated a row yet, a rollback, a branch that moved faster than the doc), **the source code is authoritative** until this document is corrected. When you find a disagreement, fix the row — don't act on the stale status and don't treat the code as wrong because this file says otherwise.

---

## Snapshot

| | |
|---|---|
| Current phase | Implementation Phase 1 (per `PROJECT_CONTEXT.md`) — RM-01 through RM-10 complete and merged into `develop`, corrected per the Repository Integration Audit (see note below); RM-11 Phase 0 also merged |
| Repo stage | RM-01 through RM-10 complete and merged into `develop`, plus RM-11 Phase 0/1/2 (DeepStream AI Pipeline). **RM-08, RM-09, and RM-10 were originally merged directly into `develop` without a presented design review or a recorded Principal Engineer review** — a deviation from `CLAUDE.md`'s Standard Development Workflow that every prior milestone (RM-01–RM-07, RM-05) followed. This caused a real CI failure (RM-08) and two unreviewed architecture decisions (RM-09, RM-10); RM-08's own docs commit also **deleted** the Design Note rows for RM-03/RM-04/RM-06/RM-07/RM-05 and the Primary branches/Branching model rows from this table (restored below). A full Repository Integration Audit was conducted, reviewed, and approved; all findings were corrected (Category A: CI fix; Category B: three architecture corrections, each individually reviewed and approved). CI is green again. Full detail in the Implementation Notes entry below — do not repeat this incident's specifics elsewhere in this file. |
| Quality gates | **Fully green** as of 2026-07-23 (`develop`): 358 passed, 0 failed, 55 skipped (DB-dependent tests skip without reachable PostgreSQL, not failures), ruff/black/mypy clean repo-wide (mypy is advisory-only in CI per `.github/workflows/ci.yml`, but all 21 findings were fixed anyway, not just left green-by-policy). The repository's last remaining known failure — `tests/services/recording/test_service.py::test_disk_usage_warning_threshold`, referenced as "one pre-existing unrelated failure" throughout the RM-11 Phase 0–2 Design notes above — was a test-only async event-delivery race (`InProcessEventBus.publish()` schedules subscriber delivery asynchronously; the test asserted before the subscriber's background task ran), not a defect in `RecordingService`. Fixed on `feature/recording`, reviewed, and merged; no production code changed. First fully green verification recorded 2026-07-22; re-verified fully green again 2026-07-23 after the RM-11.SIV runbook/CI-fix/operator-tooling merge (see Recently Completed). |
| Design note (RM-11) | Phase 0 reviewed by the Principal Engineer and **APPROVED FOR MERGE INTO DEVELOP**: scope confirmed infrastructure-only (no PGIE/SGIE, no model-dependent logic); the GLib↔asyncio bridge confirmed as the single concurrency boundary (`DeepStream Runtime → Runtime Adapter → Bridge → Application Services`, no subsystem aware of GStreamer threading, no application service depending on GLib); Runtime Adapter confirmed limited to camera lifecycle/disconnect/event publication with Threat Engine/Incident/Alarm/Calibration integration correctly deferred; ingestion, health integration (consumes RM-09's `HealthCollector` without extending it), and pipeline builder each confirmed correctly scoped. Outstanding issues (pre-existing Recording Service test failure, numpy-stub/Python 3.13 mypy issue, absent DeepStream runtime libraries) confirmed external to RM-11 and non-blocking. Approved next phase: RM-11 Phase 1, scoped strictly to PGIE integration, NvDCF integration, single-camera execution, placeholder model configuration, and performance instrumentation — explicitly excluding SGIE, Threat Engine, Calibration, Incident, and Alarm integration until Phase 1 itself has been reviewed and approved. Five decisions from the RM-11 design review, approved before implementation ("APPROVED WITH REQUIRED CHANGES"): (A) **Runtime topology** — a single OS process; DeepStream/GStreamer runs its own GLib main-loop thread, the application owns one asyncio loop, and an `AsyncBridge` (`apps/deepstream/app/bridge.py`) is the *only* concurrency boundary, bridging via `asyncio.run_coroutine_threadsafe` — no IPC, preserving RM-04/RM-05/RM-07/RM-10's in-process designs. (B) **Calibration hot path** — RM-11 must not query the DB per frame, but also must not bypass `CalibrationService`; caching is `CalibrationService`'s own responsibility to add (a future `feature/calibration` change), not RM-11's — the Runtime Adapter only ever calls `estimate()`. Phase 0 does not touch calibration yet, so this is not yet implemented. (C) **Model readiness** — RM-11 proceeds without production models; Phase 0/1 use placeholder PGIE/SGIE configuration, isolated behind the pipeline-config layer, with no assumptions about labels/thresholds/TensorRT engines until `MODEL_REGISTRY.md` approval. (D) **Camera count** — 20 cameras is the authoritative target (Roadmap + ADR-010 outrank `BENCHMARK_ACCEPTANCE_CRITERIA.md`'s stale "10 per Jetson" figure); validation still starts at 1 camera and scales incrementally. (E) **Calibration acceptance** — use `BENCHMARK_ACCEPTANCE_CRITERIA.md`'s ≤2m@20m/≤5m@50m numbers until `CAMERA_CALIBRATION_SPEC.md`'s own (currently "TBD") criteria are defined; no new criteria invented during RM-11. Approved phased order: Phase 0 (ingestion/reconnect/heartbeat/bridge/Runtime Adapter skeleton) → Phase 1 (PGIE+NvDCF, single camera) → Phase 2 (SGIE+Calibration+Threat Engine+Incident+Alarm wiring) → Phase 3 (multi-camera scaling) → Phase 4 (soak/perf validation). Constraint: infrastructure first, integration second, optimization last — no throughput optimization before single-camera correctness is verified. |
| Design note (RM-11, SIV) | RM-11.SIV (System Integration Validation) — explicitly approved as an addendum under RM-11, **not** RM-11 Phase 3 (multi-camera scaling) and not feature development. Implemented on ticket branch `feature/RM-11-SIV` (cut from `feature/deepstream`, per the Git Branching & Merge Strategy's short-lived ticket branch pattern). Delivers, per the approval's 10 tasks: `configs/models.yaml`/`ModelConfigResolver` (external PGIE/SGIE model configuration replacing Phase 1/2's placeholder-config mechanism, falls back to the original placeholder configs when a stage is `enabled: false`); `scripts/siv_register_camera.py` (camera bootstrap — encrypts and upserts `cameras`/`camera_stream_profiles`, `DeepStreamRuntime` still loads exclusively from the DB via the existing `CameraRegistry`, never from `configs/camera.yaml` directly); `stage_logging.py` (17 named `radar_eye.stage.*` loggers plus `radar_eye.audit`, an operator-facing logger for major events only); `heartbeat_registry.py` (`HeartbeatRegistry`/`HeartbeatStatus` — the single source of truth both the watchdog and dashboard read from, per the approval's Unified Heartbeat requirement); `pipeline_trace.py` (`PipelineTracer` — full frame-execution trace, off by default, near-zero overhead when disabled); `siv/watchdog.py` (`Watchdog` — visibility only, never recovery, per-component staleness detection with audit-log warnings on the healthy→stale falling edge); `siv/dashboard.py` (`Dashboard` — console status table, more than healthy/unhealthy per component); throughput counters (PGIE/SGIE fps, event/threat/alarm/incident per-second) added to the existing `PerformanceInstrumentation`; `siv/report.py` (`generate_siv_report` — automatic `siv_report.json` per run, the baseline artifact for RM-11 Phase 3's future camera-count scaling comparisons); `scripts/run_siv.py` (the SIV composition root — builds the same `DeepStreamRuntime` `main.py` builds, plus watchdog/dashboard on top; no production orchestration module imports anything from `apps/deepstream/app/siv/`, preserving that package's documented consumer-only boundary); `docs/SIV_VALIDATION_REPORT.md` (the Task 9 checklist, filled in with real hardware-run evidence). **Two real defects found and fixed via this milestone's own real-hardware verification** (exactly the class of black-box failure SIV exists to catch): (1) `nvstreammux` had no `batched-push-timeout` set, pre-existing since RM-11 Phase 0 — with fewer active sources than `batch-size` (true for every camera count below 20, in particular SIV's one-camera case), the pipeline never left PAUSED and zero frames ever flowed; fixed with a new `DeepStreamSettings.streammux_batched_push_timeout_us` field (40000, matching the common DeepStream reference-app default); (2) `Watchdog`'s `pipeline_fps` check initially derived its own internal state instead of beating the shared `HeartbeatRegistry`, so the Dashboard (which only ever reads that registry) showed it permanently stalled even when the Watchdog itself reported it healthy — fixed by having `RuntimeAdapter.on_frame_observation` beat `pipeline_fps` (and `camera`, a related gap — it previously only beat once at `on_camera_connected`) directly, making both plain beat-fed components like every other one. **Hardware verification** (same local-`GstRtspServer` + placeholder-model methodology as RM-11 Phase 1/2, real GPU/DeepStream/TensorRT/pyds): after both fixes, `rtsp`/`pgie`/`tracker`/`sgie`/`runtime_adapter`/`threat_runtime_adapter`/`camera`/`pipeline_fps` all confirmed alive with real frame counts (65-69 frames, ~26fps, ~96-110ms latency, real GPU/memory readings); Dashboard and Watchdog confirmed consistent; audit/stage logging confirmed firing; `siv_report.json` confirmed generated with real data. Calibration/ThreatEngine/Incident/Alarm orchestration (DB-dependent) verified via `test_threat_runtime_adapter.py`'s DB-backed unit tests (skip without reachable PostgreSQL, unexecuted in this sandbox this session) rather than the hardware run itself. Full detail and PASS/FAIL/Evidence per checklist item: `docs/SIV_VALIDATION_REPORT.md`. **Follow-up (2026-07-23): real-model integration path reviewed and extended.** The Principal Engineer pointed at a pre-existing, out-of-repo working DeepStream-Yolo build (`~/Downloads/DeepStream-Yolo/` — see the Blocking Issues note already on record about this directory) and asked for its required imports to be reviewed. Found: Model-001/Model-002 (`docs/MODEL_REGISTRY.md`) had already been exported to ONNX and built into working FP16 TensorRT engines outside this repo, and Model-001 (the custom-trained detector) requires a custom `nvinfer` bbox-parser plugin RM-11.SIV's config surface had no way to express (`ModelConfigResolver` could not have loaded this model at all). Vendored the plugin's *source only* (`apps/deepstream/native/nvdsinfer_custom_impl_Yolo/`, MIT-licensed, from `marcoslucianops/DeepStream-Yolo` — never its compiled output or any model/engine binary) with a build script (`scripts/build_yolo_parser.sh`); extended `ModelStageSettings`/both templates/`ModelConfigResolver` with the real properties a working config actually needs (`custom_lib_path`, `parse_bbox_func_name`, `cluster_mode`, `network_type`, `maintain_aspect_ratio`, `symmetric_padding`, `topk`, `operate_on_class_ids`, `input_object_min_width`/`height`, `infer_dims`) — all optional, so the placeholder/stock-model path renders identically to before. Along the way, found and fixed a second real defect in the *original* RM-11.SIV templates: `infer-dims` was unconditionally hardcoded to a guessed value on both stages, when a real working config omits it entirely and lets nvinfer auto-detect the model's true input shape from the ONNX graph — the guess could have silently mismatched the real model. Verified (not merely built): compiled the vendored `.so` locally (confirms `NvDsInferParseYolo`/`NvDsInferYoloCudaEngineGet` symbols export correctly) and confirmed `ModelConfigResolver`, pointed at the real `yolo26m_weapon.onnx`/`vit_binary.onnx`/engine/label paths, renders output equivalent to the Principal Engineer's independently hand-authored working `config_infer_primary_yolo26.txt`/`config_infer_secondary_vit.txt`. The checked-in `configs/models.yaml` remains on safe generic defaults (`enabled: false`) — per `MODEL_REGISTRY.md`'s standing rule, these models are still unbenchmarked and this review does not change that; real paths belong in a local, gitignored `configs/models.local.yaml` override, not the tracked file. |
| Design note (RM-11, SIV Visualization) | Operator-visible real-time video output — approved as an addendum under RM-11.SIV, not RM-11 Phase 3. Implemented on ticket branch `feature/RM-11-SIV-visualization` (cut from `feature/deepstream`), **not yet merged**. Architecture (frozen after 6 rounds of Principal Engineer review before implementation began): renders directly from the existing PGIE/NvDCF/SGIE pipeline's own inference results — no second inference pass, no OpenCV, no duplicate metadata extraction. A `tee` immediately after SGIE exists only when `configs/visualization.yaml`'s `enabled: true`; when `false` (default) the pipeline is byte-for-byte identical to pre-visualization behavior. New package `apps/deepstream/app/visualization/`: `VisualizationManager` (the sole external boundary — `initialize()`/`start()`/`stop()`/`health()`/`track_annotations`), `VisualizationPipelineBuilder` (constructs `viz-queue → nvvideoconvert → capsfilter(RGBA) → [annotate probe] → nvdsosd → nvvideoconvert → capsfilter(NV12) → nvv4l2h264enc → h264parse → rtph264pay → udpsink`), `DeepStreamOverlayRenderer` (the only `pyds`-touching code in the package, mirroring `RuntimeAdapter`'s existing sole-boundary pattern; writes only `rect_params`/`text_params`/`NvDsDisplayMeta` — never a detection/classification/tracking field), `overlay.py` (pure color/text logic, zero `gi`/`Gst`/`pyds` imports, 100% unit-tested), `TrackAnnotationRegistry` (TTL-evicting `(camera_id, track_id) → zone/distance/threat_level` store, written by `ThreatEngineRuntimeAdapter` after calibration/threat-engine results exist, read by the renderer — bridges the fact that those results aren't available synchronously inside the pad probe that produced the frame), `RtspStreamServer` (`GstRtspServer` proxy of the already-encoded UDP stream, `set_shared(True)` so multiple clients share one relay). Backpressure: `viz-queue` is `leaky=2`/`max-size-buffers=4` — the branch can never block the `tee` and therefore can never stall inference. Failure isolation: `builder.py`'s single `try/except` around `initialize()`/`start()` converts any construction/RTSP failure into `VisualizationManager.health()` reporting `enabled=True, running=False, reason=<...>` — inference continues regardless. **One real defect found and fixed during Phase 5 hardware verification**: RTSP clients saw solid green/gray frames indefinitely. Systematically ruled out (each individually disabled/bypassed and re-tested on real hardware): the annotate probe, `nvdsosd`, RGBA intermediate format, tee fan-out, queue leaky/max-size settings, `nvbuf-memory-type` mismatch, `nvvideoconvert` passthrough mode alone. Root cause isolated via a raw-frame-dump diagnostic tapping the pipeline immediately after OSD compositing (bypassing encode/RTSP/decode entirely) — the frame was always correct, proving the corruption was in the encode/RTSP/decode round-trip, not convert/OSD: `nvv4l2h264enc`'s default `iframeinterval` let a client join mid-GOP and decode only P-frames against a reference it never received, the classic "solid color until the first IDR arrives" artifact. Fixed by setting `iframeinterval` to `output_fps` (one keyframe/sec, matching `rtph264pay`'s existing `config-interval=1`); confirmed via a real RTSP client capture showing a correct, live-annotated frame (bounding box, track ID, SGIE classification). The `disable-passthrough`/`nvbuf-memory-type=2` hardening applied while investigating remains valid defense-in-depth but was not the actual root cause. **Hardware verification** (real camera, real production PGIE/SGIE models — `configs/models.local.yaml`, gitignored): RTSP client SETUP/PLAY confirmed against `rtsp://<host>:8554/radar-eye`; failure isolation confirmed by pre-binding `rtsp_port` and observing inference continue at baseline FPS/GPU/CPU while `VisualizationManager.health()` correctly reported the failure; OFF-vs-ON performance comparison (mean of 10 steady-state samples each) recorded in `docs/SIV_BASELINE.md` — no measurable inference FPS impact, +0.23ms latency, +~1GB GPU memory, +~5 GPU utilization points. Construction-time failure (`ElementFactory.make()` returning `None`/`link()` returning `False`) shares the identical `try/except` path but was not independently reproduced on real hardware this session — every element factory name is hardcoded and confirmed present on this machine; forcing that path would require editing the frozen implementation, out of scope. 9 commits, each its own quality-gated phase (`54ca0cb` Phase 1 settings/overlay/registry → `6256593` Phase 2 renderer/builder/manager → `edc3333` Phase 3 RTSP server → `0e8ce9b` Phase 4 runtime wiring → `9d8ee9f`/`9ab4764` the pay0/NVMM/iframeinterval fixes → `0894a04`/`e1656c7` docs). Full repo quality gates green: 395 passed, 55 skipped, ruff/black/mypy clean. Docs updated: `docs/DEEPSTREAM_PIPELINE_SPEC.md` (Stage 5.5, fork-point diagram, backpressure/immutability rules as named constraints) and `docs/SIV_OPERATOR_RUNBOOK.md` (step 6.5 — config, RTSP URL, expected overlay, verification checklist, failure-mode/recovery table). Not yet merged into `feature/deepstream`/`develop` — per explicit instruction, no merge until every quality gate passes (they do) and the Principal Engineer authorizes the merge. |
| Design note (RM-11, Phase 2) | Phase 2 implemented on `feature/deepstream` and hardware-verified, per the approved implementation order (SGIE → ThreatEngineRuntimeAdapter → Calibration → Threat Engine → Incident → Alarm → Event publication → hardware verification). Delivers: `sgie` (`nvinfer`, secondary mode) attached after `nvtracker`, config `apps/deepstream/configs/sgie_placeholder.txt` (DeepStream's stock vehicle-type classifier, operating on the placeholder PGIE's "car" class — Decision B); `DetectionObservation.secondary_label` (raw passthrough, never mapped to `UniformClass`); `apps/deepstream/app/threat_runtime_adapter.py` (`ThreatEngineRuntimeAdapter`) — a component distinct from ADR-027's `RuntimeAdapter`, matching RM-07/09/10's own naming, orchestration-only per the design review's Additional Requirement (no business rules of its own; `weapon_mapper`/`uniform_mapper` are injected, defaulting to the honest NONE/UNKNOWN values rather than a fabricated placeholder-model guess). Flow: `CalibrationService.estimate()` (now cached — see Design note (RM-05)) → `ThreatEngine.ingest()` → `EscalationSignal` routed to `IncidentService.handle_escalation()` (fresh short-lived session per call, Decision D — no session on the per-frame path) and, for `ALARM_ELIGIBLE`, also `AlarmService.trigger()` (a long-lived singleton owned by `DeepStreamRuntime`, since its in-memory alarm-record state must persist across calls, unlike Incident/Calibration; its fail-safe `stop()` is wired into `DeepStreamRuntime.stop()`) → `ThreatAssessmentEvent`/`HumanReviewItemCreatedEvent` published on the real `EventBus`. **Hardware verification** (30s run, real RTSP ingestion, real PGIE+NvDCF+SGIE inference): zero pipeline errors; SGIE genuinely classified real detections (five distinct vehicle-type labels observed in `secondary_label`, proving real classifier metadata flow, not just element construction); tracking unchanged from Phase 1 (130 distinct tracks, 117 persisting ≥3 frames); 5582 `ThreatAssessmentEvent`s published (one per detection, per RM-06's per-frame reporting design); 64 `AlarmService` activations. `CalibrationService`/`IncidentService` were mocked at the class level for this run (no reachable PostgreSQL in this sandbox — their own persistence logic is covered by this commit's DB-backed unit tests, which skip here but run for real wherever Postgres is reachable; orchestration call sequencing/arguments were independently confirmed correct via a separate, tightly-controlled mocked-DB smoke test). A `weapon_mapper` forcing `WeaponType.FIRE` periodically was used to guarantee the Incident/Alarm path fires deterministically (Decision C's approved "controlled/synthetic input path") — not a claim of real threat-detection accuracy; per Decision C, that remains blocked on production models. **Latency instrumentation follow-up (Principal Engineer review, resolved)**: the suspiciously low `end_to_end_latency_ms` above was investigated per the review's required steps, not assumed. A real-hardware diagnostic compared `hash(gst_buffer)`/`id(gst_buffer)` against buffer PTS (a GStreamer-level, Python-object-identity-independent ground truth) at the streammux ingress probe and the SGIE egress probe: **15/15 matches** — the buffer-identity correlation mechanism itself is completely reliable; GStreamer's zero-copy in-place processing (ADR-005) holds through PGIE/tracker/SGIE exactly as expected, and required no change. The actual root cause: `PerformanceInstrumentation.snapshot()` reported only the single most-recently-recorded frame's latency, not a representative figure. The same diagnostic's raw per-frame measurements showed true latency ranges from ~120ms during the first few frames after `PLAYING` (draining the initial RTSP/decode queue backlog) down to a stable ~2ms once steady-state streaming begins — a single-sample reading is highly sensitive to exactly when it happens to be taken, explaining why Phase 1 (~25ms) and Phase 2 (~1.6ms) looked inconsistent despite Phase 2 doing strictly more processing. Fixed by windowing latency the same way FPS was already windowed (`_LATENCY_WINDOW_SIZE`, mirroring `_FPS_WINDOW_SIZE`) — `end_to_end_latency_ms` is now a rolling average. No application/orchestration behavior changed ("repair only the instrumentation," per the review). **Re-validated on hardware**: re-ran the full Phase 2 verification — `end_to_end_latency_ms` now reads **1.87ms**, stable and consistent with the diagnostic's own steady-state measurement; every other result (SGIE labels, tracking, event/alarm counts, zero pipeline errors) was unchanged between runs, confirming no application behavior changed. 22 new/updated unit tests total (19 Phase 2 + 3 for the rolling-average fix), all passing; ruff/black clean; full repo suite green except the same one pre-existing, unrelated Recording Service failure. |
| Design note (RM-11, Phase 1) | Phase 1 implemented on `feature/deepstream` and verified end-to-end on physical hardware (this machine's reference environment). Delivers: `nvinfer` (PGIE) attached after `nvstreammux`, configured via `apps/deepstream/configs/pgie_placeholder.txt` (DeepStream's stock `resnet18_trafficcamnet` sample detector — explicitly not the project's own weapon model, per Decision C); `nvtracker` (NvDCF) attached immediately after PGIE; `apps/deepstream/app/observations.py` (`FrameObservation`/`DetectionObservation`/`BoundingBox`, the repository-native domain objects ADR-027 names); `RuntimeAdapter.extract_frame_observations()`/`_extract_raw_detections()` as the only `pyds`-importing code in the repository, reducing `NvDsObjectMeta` to plain primitive tuples immediately before handing off to the pure, independently-unit-tested `build_frame_observation()`; `apps/deepstream/app/instrumentation.py` (`PerformanceInstrumentation`) collecting all eight required metrics. **Caught and fixed a real bug during implementation**: an early version fed `time.monotonic()` values into `datetime.fromtimestamp()` for `FrameObservation`'s timestamp fields — monotonic clocks have no epoch relationship to wall-clock time, so this silently produced meaningless dates (the delta math for latency happened to stay correct by cancellation, but the timestamps themselves were garbage). Fixed by strictly separating the two clocks: `ingress_timestamp`/`metadata_timestamp` are always real `datetime.now(timezone.utc)` values; latency/FPS measurement uses `time.monotonic()` exclusively, passed as separate explicit arguments, never derived from the domain object's fields. **Hardware verification results** (30s run, single camera via a locally-hosted test RTSP stream, sample_qHD.mp4 at native 25fps): `PLAYING` reached with zero pipeline errors; PGIE loaded and ran real inference (750 frames processed, 5618 detections); NvDCF assigned persistent track IDs (130 distinct tracks, 117 — 90% — persisting ≥3 frames, demonstrating genuine tracking rather than per-frame re-identification); `RuntimeAdapter.last_observation` confirmed as a real `FrameObservation` (not a `pyds`/`Gst` type) with the correct `camera_id`; steady-state `inference_fps` 25.4 (matches source), `end_to_end_latency_ms` ~25ms. **Caveat**: `pipeline_startup_seconds` measured ~585s in this run — an artifact of the placeholder model requiring a fresh on-the-fly TensorRT INT8 engine build (the shared DeepStream SDK samples directory this placeholder config points at is not writable by this environment's user, so the built engine could not be cached to disk, confirmed via a `Serialize engine failed... opened error` warning) combined with GPU contention from an unrelated process already running on this shared dev machine — not representative of steady-state or of the real (not-yet-benchmarked) production model, and not a code defect. `sample_system_metrics()`'s GPU/CPU/memory readers were separately confirmed working against this real machine (live values: ~99% GPU util, 917MB/12288MB GPU memory, ~12% CPU, ~33% system memory) — the periodic-task *wiring* inside `DeepStreamRuntime._sample_metrics_forever()` itself was not exercised in this hardware run, since going through the full `DeepStreamRuntime.start()` requires the DB-backed `CameraRegistry` (no reachable PostgreSQL in this environment; unchanged Phase 0 code, not re-verified here) — the verification instead constructed `DeepStreamPipeline`/`RuntimeAdapter`/`AsyncBridge` directly with a manually-built `CameraSource`, real repo code throughout except that one substitution. 31 new unit tests, all passing; ruff/black clean; full repo suite green except the same one pre-existing, unrelated Recording Service failure. |
| Design note (RM-11, Runtime Verification) | Runtime Verification Review: **APPROVED**. Confirms, on physical hardware (see Reference environment (RM-11) row and Implementation Notes), that the repository architecture operates correctly against a real DeepStream environment — DeepStream runtime, TensorRT inference, GStreamer integration, `NvDsBatchMeta` extraction, Runtime Adapter integration, `AsyncBridge` execution, and `EventBus` delivery all verified end-to-end. Retires the principal technical risk identified before RM-11. **New architectural constraint, effective immediately**: the Runtime Adapter is the anti-corruption layer between the NVIDIA SDK and the application's domain model — DeepStream/`pyds`-specific types (`pyds` types generally, `NvDsBatchMeta`, `NvDsFrameMeta`, `NvDsObjectMeta`) must never propagate beyond the Runtime Adapter; every downstream service (Threat Engine, Calibration, Incident, Alarm, Event Bus subscribers) receives only repository-native domain objects (`shared/constants`, `shared/events` types, plain Python values). No service outside RM-11 should require knowledge of DeepStream or `pyds`. This governs Phase 1/2 implementation going forward; nothing in current Phase 0 code touches `pyds` types (the RM-11 environment-verification smoke test parsed `NvDsBatchMeta` in a standalone pad-probe callback and handed the Runtime Adapter only a plain string summary, already consistent with this constraint) — pyds-specific parsing does not exist in the Runtime Adapter as shipped and must be added, when Phase 1/2 need it, entirely inside the Runtime Adapter's own module boundary. **This constraint was subsequently formalized as ADR-027** (`ADR_INDEX.md`) — a cross-milestone, all-subsystems architecture invariant (expanded type list including `NvDsClassifierMeta`/`NvDsUserMeta`/`Gst.Buffer`/`Gst.Sample`; explicit import restriction on `pyds`/`gi.repository.Gst`/`gi.repository.GLib` outside `apps/deepstream/`), not an RM-11-scoped implementation choice. ADR-027 is now the authoritative source for this boundary per `CLAUDE.md`'s priority order; this row is retained as the review-history record. |
| Design note (RM-10) | Retroactive architectural decision from the Repository Integration Audit review (RM-10 was originally implemented without a design review): `AlarmService`'s integration boundary is a direct in-process call (`trigger(camera_id, track_id, incident_id, threat_level, reason, timestamp)`), not a bus subscription to `AlarmRequestedEvent`. The original implementation subscribed to that event, but nothing in the system produces it — the same gap RM-07's design review resolved for Incident Service via a direct call (`handle_escalation()`, "no bus"). The future Threat Engine Runtime Adapter (RM-11) now has one consistent hand-off shape for both of its outputs. `AlarmRequestedEvent`/`AlarmRequestedPayload` remain valid shared contracts (`EVENT_CONTRACTS.md`) but are not this service's integration point. |
| Design note (RM-09) | Two retroactive corrections from the Repository Integration Audit review (RM-09 was originally implemented without a design review): (1) `shared/schemas/health.py` ownership was reassigned to `feature/shared-contracts`, matching every other `shared/` addition (RM-02, RM-04) — no exception is granted for API-response-shape-only schemas; (2) the REST routes originally used an undocumented `/api/v1` prefix and omitted the documented `GET /health/recording` endpoint. Routes now match `FRONTEND_BACKEND_CONTRACTS.md` exactly (`/health/system`, `/health/gpu`, `/health/storage`, `/health/recording`, `/health/cameras`, `/cameras/{id}/health`); introducing an API version prefix remains an open, undecided question requiring its own ADR if raised again. |
| Design note (RM-05) | Two design decisions from the RM-05 design review, approved before implementation: (1) calibration history is append-only (no `status`/`is_current` column, none to be added) — "current calibration" for a camera is defined as the row with the greatest `created_at` for that `camera_id`; recalibration always inserts a new row, never mutates or deletes prior ones; (2) no reprojection-error acceptance gate — `calibrate()` validates only structural input (>=4 reference points, a numerically well-determined/non-degenerate homography) and, once a mathematically valid homography is computed, persists it unconditionally. Calibration quality against real-world measurements is a future field-validated concern (a later ADR or milestone), not RM-05's. RM-05 has no direct coupling to the Threat Engine or Incident Service: per `EVENT_CONTRACTS.md`, `CalibrationUpdatedEvent`'s only consumer is DeepStream, and `DEEPSTREAM_PIPELINE_SPEC.md`'s Stage 6 is the in-process integrator that will call `estimate()` per frame and feed the Threat Engine (RM-11) — the same deferred-integration shape approved for RM-07's Threat Engine Runtime Adapter. **RM-11 Phase 2 prerequisite (implemented and merged):** `CalibrationService` now owns a process-wide, module-level in-memory homography cache (per the RM-11 Phase 2 design review's Decision A — "Phase 2 shall instead depend on the approved CalibrationService caching implementation") — `calibrate()` writes it immediately, `estimate()` reads from it and only falls back to the database on a cold miss. `estimate()` remains the only public API RM-11 calls; no calibration logic moved into RM-11. |
| Design note (RM-07) | Two design decisions from the RM-07 design review, approved before implementation: (1) escalation logic ownership — Threat Engine remains the sole owner of debounce/hysteresis/sustained-duration timing (ADR-021); Incident Service never re-derives timing from the `ThreatAssessmentEvent` stream, it only consumes that stream for `threat_summary` metadata and track-lost ("last seen") bookkeeping; (2) the hand-off mechanism — rather than a new canonical event (which would modify `EVENT_CONTRACTS.md`, a Priority-3 architecture document, prematurely) or Incident Service re-implementing timing itself, a **Threat Engine Runtime Adapter** (belongs to the Threat Engine subsystem, not Incident Service) will observe `EscalationSignal` and invoke Incident Service's public `handle_escalation(camera_id, track_id, threat_level, reason, timestamp)` entry point directly (in-process, no bus). That adapter is **explicitly deferred** until a real per-frame pipeline exists to feed it (RM-11) — RM-07 only builds and tests Incident Service's side of that interface. |
| Design note (RM-06) | Two design decisions from the RM-06 design review, approved before implementation: (1) rule precedence — FIRE overrides uniform-based rules (a fire hazard applies regardless of allegiance), so `military`/`unknown` + `fire` → HIGH, not ALLY/HUMAN_REVIEW; (2) `AlarmRequestedEvent`'s documented producer (Threat Engine, per EVENT_CONTRACTS.md) has no documented way to obtain the `incident_id` its payload requires, since `IncidentCreatedEvent` is produced by Incident Service and Threat Engine isn't a documented consumer of it. RM-06 therefore emits an internal `EscalationSignal` (`INCIDENT_ELIGIBLE`/`ALARM_ELIGIBLE`, no `incident_id`) instead of constructing literal `IncidentCreatedEvent`/`AlarmRequestedEvent` instances. Now resolved by RM-07's design (see Design note (RM-07)) via the deferred Threat Engine Runtime Adapter — the real event assembly still awaits that adapter's construction (RM-11). |
| Design note (RM-04) | Per the RM-04 design review: delivery is best-effort/at-most-once per subscriber (no durable log); ordering is FIFO per producer per event type only (no cross-producer guarantee — sort by the envelope's `timestamp` if needed, per INV-014); back-pressure policy (explicit decision) — each subscriber has a bounded queue, `publish()` waits up to a configurable timeout (default 1s) for space, then drops for that subscriber only and emits a CRITICAL `SystemEvent` with subscriber identity/event type/queue depth/drop count; one subscriber's handler raising never affects other subscribers or the publisher; no replay capability (consumers reconstruct state from the database, per ADR-008). `EventBus` is an abstract contract with `InProcessEventBus` as the swappable initial implementation — same pattern as RM-03's `CredentialEncryptionProvider` — so a distributed transport can replace it later without touching producers/consumers. Explicitly out of scope: dynamic queue resizing, priority queues, dead-letter queues, replay, subscriber eviction. |
| Design note (RM-03) | Per the RM-03 design review: persistence ownership is exclusive to `apps/api` (`docs/REPOSITORY_ARCHITECTURE.md`'s Ownership Rules) — no other subsystem defines ORM models, migrations, or holds a DB session; Threat Engine remains DB-independent, and future subsystems (Incident Service, etc.) will communicate through repositories and events, not direct DB access. Credential encryption uses the `CredentialEncryptionProvider` abstraction (not a direct Fernet dependency) so the concrete implementation is replaceable later. `users.role` and `camera_stream_profiles.transport` are plain `str` columns — no architecture document defines a value set for either yet; left unconstrained rather than inventing a taxonomy. |
| Reference environment (RM-11) | This development machine confirmed, by direct verification (not assumption), to have a working NVIDIA/DeepStream runtime: DeepStream SDK **7.0.0**, GStreamer **1.20.3**, CUDA **12.2** (driver 535.288.01), TensorRT **8.6.1** (`libnvinfer` 8.6.1.6), GPU **NVIDIA GeForce RTX 3060** (12GB) — see the Implementation Notes entry below for the full detection method and a minimal end-to-end pipeline smoke test (`nvstreammux` load → PLAYING → PGIE load/inference → `NvDsBatchMeta` parsed via `pyds` → real `AsyncBridge`/`RuntimeAdapter`/`InProcessEventBus` from `apps/deepstream/app`, all Phase 0 code, unmodified). `pyds` and `PyGObject` were not present under either Python interpreter on this machine before this session; `pyds` 1.1.11 was installed user-locally under the system Python (`/usr/bin/python3`, 3.10 — the interpreter DeepStream 7.0/Ubuntu 22.04 actually binds `gi`/`Gst` against; the `miniconda` Python 3.13 this repo's own test suite runs under still has neither `gi` nor `pyds` and is not expected to). This machine is now the reference development environment for RM-11 per the repository-environment-update instruction that requested this verification — **not** a stand-in for the Jetson AGX Orin deployment target; the `RM-11 (DeepStream AI Pipeline) sizing` row in Blocked below is about Jetson-specific compute budget and is unaffected by this. Model readiness (`MODEL_REGISTRY.md`) is also unaffected: the smoke test deliberately used DeepStream's own stock sample detector (`resnet18_trafficcamnet`), not the project's own weapon/uniform models, per the RM-11 design review's Decision C. |
| Primary branches | `develop` (integration branch, protected — no direct commits outside reviewed subsystem merges). `main` (production) does not exist yet — created only at the first full production release, per the Production Release Gate in `CLAUDE.md`. |
| Branching model | `main` ← `develop` ← long-lived subsystem branches ← optional short-lived ticket branches. `develop` is the active integration branch every subsystem milestone merges into after review; `main` receives merges from `develop` only at a validated production release. See Subsystem Status below for current branches, and `CLAUDE.md`'s "Git Branching & Merge Strategy" for the full model. |

---

## Repository Milestones

Durable log of significant whole-repository achievements — distinct from `Milestone Status` below (per-`RM-XX` roadmap progress) and from `Recently Completed`/`Changelog` (chronological work logs). An entry here marks a repository-wide state worth remembering on its own, not tied to one milestone's completion.

| Date | Milestone |
|---|---|
| 2026-08-03 | **Live Monitoring dynamic AI switching shipped (Stage C) and Lifecycle removed from the product entirely.** Stage C added the operator-facing Runtime AI Enable/Disable experience on top of the already-validated WebRTC transport: a second, annotated H.264 input feeds the existing `input-selector`, switched purely in GStreamer (no reconnect/renegotiation/new PeerConnection), hardware-validated in both directions. Camera re-registration (delete a camera, register the same physical camera again) was found broken -- root-caused to `DesiredStateSynchronizer`'s ongoing convergence loop never calling `RuntimeAdapter.on_camera_connected` for a camera added after startup -- and fixed. Lifecycle (`DRAFT/TESTING/VERIFIED/OPERATIONAL/MAINTENANCE/DISABLED`) was then removed from the product as a final architectural decision: exactly two operator controls remain per camera -- registration/deletion (implicit connect/disconnect) and AI Enable/Disable. See the corresponding Recently Completed entry for full detail. |
| 2026-07-30 | **Camera Connectivity decoupled from Lifecycle/AI Runtime ownership**: Operator Acceptance Testing found a registered camera never connected until promoted all the way to `OPERATIONAL` (3 explicit lifecycle transitions) -- coupling Camera Connectivity to AI Runtime ownership through `lifecycle_state` as an intermediary. Fixed as a minimal, targeted policy change (not a redesign): `DefaultLifecycleSourcePolicy` in `apps/deepstream/app/synchronization.py` now has two independent methods -- `should_have_active_source` (every lifecycle state except `DISABLED` connects) and `should_allow_ai` (unchanged: only `OPERATIONAL` runs AI) -- so a camera connects immediately on registration while AI eligibility stays exactly as before. `RuntimeSupervisor`/`DesiredStateSynchronizer`/`MediaPublisher`/`DeepStreamPipeline` architecture untouched, per explicit direction. Also fixed: Observed State (`status`/`fps`/`latency_ms`/`last_seen_at`/`reconnect_count`/`last_stream_error`) is now actually persisted to Postgres -- previously tracked only in two separate processes' in-memory state (`RuntimeAdapter` in `apps.deepstream`, `HealthCollector` in `apps.api`), never fed across the process boundary, so `GET /health/cameras` always returned nulls. `RuntimeAdapter` now writes connection-state transitions immediately (event-driven); `HeartbeatScheduler` writes fps/latency at a coarser, configurable interval (`observed_state_flush_interval_seconds`, default 3s) to keep database write volume low. Hardware validation surfaced and fixed one real, migration-caused crash (`runtime.py`'s `_schedule_reconnect` assumed every camera's `ReconnectPolicy` was pre-populated at startup, which is no longer true now that cameras can be added post-startup far more commonly) and surfaced-but-did-not-fix one pre-existing, orthogonal defect (the reconnect retry loop has no exit condition tied to Desired State -- transitioning to `DISABLED` does not stop an in-flight reconnect chain), explicitly logged as a known gap in `docs/CAMERA_RUNTIME_LIFECYCLE.md` §6 rather than bundled into this fix. See the corresponding Recently Completed entry for full detail. |
| 2026-07-29 | **Camera Management operator workflow complete**: the Cameras page is now the complete Camera Management interface -- register/edit/delete cameras entirely from the browser, no curl/SQL/terminal required. Backend: `POST/PATCH/DELETE /cameras`, `GET /cameras/brands`, an extensible RTSP URL generator service (`apps.api.app.services.rtsp_url_generator`, one adapter per brand -- Hikvision/Dahua/Uniview/Axis/Hanwha) so the operator never types a raw RTSP URL, and a `model` field added to `camera_stream_profiles` (a gap found against the operator spec during frontend implementation). Frontend: full camera table (name/brand/IP/location/lifecycle/AI/recording/connection status/health/last seen/actions), Add/Edit Camera forms (react-hook-form + zod, brand-driven port/stream defaults), a lifecycle-transition control, and a delete confirmation. Confirmed already-satisfied without further work: Settings' AI Model/Notifications/Audit Log tabs already show a professional `DisabledFeaturePanel` "not yet available" state (no bug-looking blank screens), Recording Policy is already a fixed read-only display per CLAUDE.md, and Tactical Map is already a real, honest, backend-integrated feature (not a stub, contrary to the initial assumption that it needed a "Coming Soon" state). See the corresponding Recently Completed entry for the full per-file record and the Frontend Audit Matrix. |
| 2026-07-29 | **Camera Runtime v1 activated in production**: `apps.deepstream.app.main` (the real production entrypoint, not `run_siv.py`) now constructs and runs `RuntimeSupervisor`/`DesiredStateSynchronizer`/`TelemetryCollector`/`MediaPublisher` — the completed Steps 1–7 blueprint was implemented but never wired into `runtime.py` until this integration. Also found and permanently fixed, on real hardware, a process-crashing defect in `DeepStreamPipeline.remove_source()` (segfault in NVIDIA's `libgstnvvideoconvert.so` when removing a camera while Visualization + Media Publisher were both active) — root-caused to dynamic `release_request_pad()` calls on `nvstreammux`/`nvstreamdemux`, which NVIDIA's own reference implementation and multiple Developer Forum threads document as unsafe at runtime. See the corresponding Recently Completed entry for full detail. |
| 2026-07-29 | **Camera Runtime v1 feature-complete**: all 7 steps of the frozen RM-12 Camera Runtime Implementation Blueprint (Source Manager, Frame Distributor, Runtime Supervisor, Desired State Synchronization, Telemetry, AI Runtime reorganization, Media Publisher) implemented, hardware-validated on real camera(s)/models, and merged to `develop`, each as its own commit. Closes the Camera Runtime implementation effort; the project now transitions from implementation to production hardening. See the corresponding Recently Completed entry for the full per-step record. |
| 2026-07-22 | **First fully green repository validation achieved**: 278 passed, 0 failed, 49 skipped (DB-dependent tests skipping without reachable PostgreSQL, not failures), ruff/black clean repo-wide, on `develop`. Reached after fixing the repository's last remaining test failure — a test-only async event-delivery race in `tests/services/recording/test_service.py::test_disk_usage_warning_threshold` (RM-08), not a production defect. See the Snapshot "Quality gates" row and the corresponding Recently Completed entry for full detail. |

---

## Next Immediate Action

**Camera Runtime v1 is feature-complete, active in production, and `origin/develop` is now fully synchronized** (all 7 blueprint steps implemented, hardware-validated, and — as of 2026-07-29 — pushed to GitHub; `develop` and `origin/develop` are identical, confirmed via a project-consolidation audit that found 19 commits had been sitting on local `develop` only, never pushed). A real, process-crashing hardware defect in `remove_source()` was found and permanently fixed during this integration — camera lifecycle transitions, RTSP reconnects, and Enable/Disable AI are now confirmed safe under repeated stress on real hardware. **Production Validation Mode** (`configs/validation.yaml`'s `production_validation_mode.enabled`, off by default) gives maximum observability (per-frame trace, DEBUG stage logging, console dashboard/watchdog) with zero measurable overhead confirmed on/off. **One-command launcher** (`run.py`, repo root) now starts the complete system — prerequisite checks, migrations, Backend API, DeepStream Runtime, Frontend, in dependency order with health waits, `--validation` flag, crash detection, and clean Ctrl+C shutdown — end-to-end verified on real hardware including a deliberate mid-run crash. Next: the first full end-to-end manual UI acceptance run.

Historical note (superseded): this row previously tracked RM-13 — Frontend Integration, which completed 2026-07-26 (see Milestone Status and the Subsystem Status Frontend row).

**Owning Branch:**
`develop` (Camera Runtime v1 was implemented directly on `develop` via small, individually-reviewed commits per step — see the 2026-07-29 Recently Completed entry).

**RM-11 and RM-12 — Both Closed and Merged into `develop`:**
RM-11 (DeepStream AI Pipeline: Phase 0, Phase 1, Phase 2, the RM-11.SIV addendum, and the RM-11.SIV Visualization addendum) is **COMPLETE and merged into `develop`** — the Visualization addendum (`apps/deepstream/app/visualization/`, real-hardware Development System Integration Validation, engineering review, Performance Optimization Program, baseline tag `rm-11-development-siv-baseline`) merged into `feature/deepstream` via PR #8 (regular merge commit `387a1ee`), then promoted `feature/deepstream` → `develop` on 2026-07-26 as part of the Repository Consolidation's approved merge order (no conflicts). RM-11 Phase 3 (multi-camera scaling) remains separate, not-yet-scheduled future work. See `docs/RM-11_COMPLETION.md` for the full closure index.

RM-12 (API Service — REST + WebSocket) is **COMPLETE and merged into `develop`**: all 6 implementation phases (JWT auth, `audit_log`/`AuditLogger`, 7 read-only routers, 5 mutating routers, WebSocket bridge for 5 of 6 channels, consolidated contract verification) merged `feature/RM-12-api-service` → `feature/api` → `develop` on 2026-07-26, per the Repository Consolidation's approved merge order (one conflict, in this file's own Milestone/Subsystem Status/Next Immediate Action/Recently Completed sections — resolved by combining both branches' entries, not discarding either, consistent with the same resolution approach used for the RM-11.SIV merge on 2026-07-23).

**Status:**
Both milestones' full implementation history (Phase 0-2, RM-11.SIV, RM-11.SIV Visualization, and RM-12 Phases 1-6) is preserved in the Recently Completed entries below, each retained from whichever branch originated it. `develop` now has, for the first time, both the complete DeepStream AI pipeline and a working REST + WebSocket API layer in front of it — closing the "no API layer merged" gap the Repository Integration Audit identified.

**Blocking Issues:**
None for RM-12's own implemented scope. Two items were deliberately descoped rather than guessed at, both recorded in `docs/OPEN_QUESTIONS.md`: `GET`/`PATCH /config` (Q-014 — no configuration persistence model exists anywhere in `docs/DATABASE_SCHEMA.md`/`docs/DOMAIN_MODEL.md`; needs its own Configuration Management milestone) and `/ws/tracking` (Q-015 — no event model, payload, or publisher exists for live tracking data anywhere; needs its own Tracking Streaming milestone). Neither blocks RM-12's remaining scope or its merge readiness. Separately, `docs/FRONTEND_BACKEND_CONTRACTS.md`'s `PATCH /users` was corrected to `PATCH /users/{user_id}` (a pre-existing documentation error — every other mutating route in that document already carries a path identifier).

RM-11 Phase 3 (multi-camera scaling, `feature/deepstream`) remains a separate, still-not-started thread — unrelated to RM-12 (whose roadmap dependencies are RM-02/RM-03/RM-04 only, per `docs/IMPLEMENTATION_ROADMAP.md`) and not blocked by anything RM-12 did. It still requires its own design review before implementation begins; see the Design note (RM-11, SIV) entry above for its own detailed blocking-issue history (model files unbenchmarked, no physical RTSP camera/reachable PostgreSQL in this sandbox, Jetson target validation still pending).
None for RM-11.SIV's own scope. Model files (`models/yolo26m_weapon.pt`, `models/vit_48k_binary.pth`) remain absent from the repo and unbenchmarked per `MODEL_REGISTRY.md` — RM-11.SIV's `configs/models.yaml` defaults both `pgie.enabled`/`sgie.enabled` to `false` (placeholder fallback) specifically because of this; swapping in the real models once approved requires only editing that file. This sandbox also has no physical RTSP camera and no reachable PostgreSQL (confirmed again during RM-11.SIV's hardware verification) — `docs/SIV_VALIDATION_REPORT.md`'s checklist records which items are PASS-via-real-hardware-run vs. PASS-via-DB-backed-unit-test-not-executed-here vs. NOT YET RUN. Final validation against the actual Jetson AGX Orin target is still required and not something this x86/RTX dev machine substitutes for. Note: this machine has pre-existing, out-of-repo TensorRT engines and benchmark artifacts under `~/Downloads/DeepStream-Yolo/` matching the project's model file names (see Implementation Notes). **Update (2026-07-23):** at the Principal Engineer's explicit direction, this directory's *integration plumbing* (a custom DeepStream `nvinfer` bbox-parser required for Model-001's architecture) was reviewed and its source vendored into `apps/deepstream/native/` — see the Design note (RM-11, SIV) follow-up entry and `docs/MODEL_REGISTRY.md`. The models themselves remain **not** MODEL_REGISTRY-approved (unbenchmarked) and the checked-in `configs/models.yaml` stays on safe generic defaults (`enabled: false`) — this update is scoped to confirming the integration path works, not to approving the models for production use; that remains whoever owns model approval's call, per `MODEL_REGISTRY.md`'s Rule.

---

## Milestone Status

Full milestone definitions, dependencies, and acceptance criteria live in `docs/IMPLEMENTATION_ROADMAP.md` — this table tracks completion status only and must not redefine what a milestone covers.

| Milestone | Status | Owning Branch |
|---|---|---|
| RM-01 | **Done** (merged into `develop`) | `feature/api` |
| RM-02 | **Done** (merged into `develop`) | `feature/shared-contracts` |
| RM-DEV | **Done** (merged into `develop`) | `feature/developer-infrastructure` |
| RM-06 | **Done** (merged into `develop`) | `feature/threat-engine` |
| RM-03 | **Done** (merged into `develop`) | `feature/api` |
| RM-04 | **Done** (merged into `develop`) | `feature/shared-contracts` |
| RM-07 | **Done** (merged into `develop`) | `feature/incident-service` |
| RM-05 | **Done** (merged into `develop`) | `feature/calibration` |
| RM-08 | **Done** (merged into `develop`; CI defect found and fixed by the Repository Integration Audit) | `feature/recording` |
| RM-09 | **Done** (merged into `develop`; ownership + route corrections per the Repository Integration Audit) | `feature/api` |
| RM-10 | **Done** (merged into `develop`; integration boundary corrected per the Repository Integration Audit) | `feature/incident-service` |
| RM-11 | **Done** (Phase 0, Phase 1, Phase 2, RM-11.SIV, and the RM-11.SIV Visualization addendum all complete and merged into `develop` — the Visualization addendum's promotion from `feature/deepstream` closed the last open gap, 2026-07-26. Phase 3 / multi-camera scaling is separate future work, not required for this completion, not yet scheduled) | `feature/deepstream` |
| RM-12 | **Done** (all 6 phases merged into `develop` via `feature/RM-12-api-service` → `feature/api` → `develop`, 2026-07-26) | `feature/api` |
| RM-13 | **Done** (Repository Consolidation: `radar-eye-command`'s full 9-commit history `git subtree`-imported into `frontend/`, merged `feature/frontend-integration` → `develop`, 2026-07-26. All 10 `UI_SCREEN_CATALOG.md` screens present, zero `mock-data.ts` references) | `feature/frontend-integration` |
| RM-14 | Not Started | `feature/deployment` |
| RM-15 | Not Started | `feature/testing` |

---

## Subsystem Status

| Subsystem | Path | Status | Branch | Depends On |
|---|---|---|---|---|
| API service (FastAPI, persistence, auth/audit, lightweight health monitoring) | `apps/api/` | **Done** (RM-01, RM-03, RM-09, and RM-12 -- JWT auth, `audit_log`, full REST surface, WebSocket bridge for 5 of 6 channels -- all merged to `develop`, 2026-07-26) | `feature/api` | Shared contracts |
| Shared contracts (events, schemas, constants, internal event bus) | `shared/` | **Done** (RM-02, RM-04 merged to `develop`) | `feature/shared-contracts` | — |
| Camera Ingestion Service (Media Architecture Reset, ADR-028 -- owns the one real RTSP connection per camera, publishes locally via the Media Distribution Interface) | `apps/ingestion/`, `shared/media_transport/` | **In Progress** (Phase 1b standalone + tee fix done; Phase 2 migrated its first real consumer -- see Recently Completed) | `feature/media-architecture-reset` | Shared contracts, API service (persistence) |
| Live Streaming Service (Media Architecture Reset, ADR-028 Phase 2 -- subscribes only to Camera Ingestion's published endpoint, owns WebRTC/signaling/browser sessions) | `apps/live_stream/`, `shared/media_transport/webrtc.py`, `shared/media_transport/webrtc_signaling.py` | **In Progress** (standalone, hardware-validated against the real camera; not yet the target of `apps.api`'s WebRTC proxy -- see Recently Completed) | `feature/media-architecture-reset` | Camera Ingestion Service (via `camera_media_endpoints`, not a direct import), Shared contracts, API service (persistence) |
| DeepStream pipeline (ingest → YOLO → NvDCF → ViT) | `apps/deepstream/` | **Done** (Phase 0/1/2 -- ingestion, PGIE/NvDCF/SGIE, instrumentation, full ThreatEngineRuntimeAdapter orchestration --, the RM-11.SIV addendum -- external model config, logging/audit logging, Unified Heartbeat watchdog/dashboard, frame tracing, camera bootstrap --, the RM-11.SIV Visualization addendum -- operator-visible RTSP video output --, and **Camera Runtime v1** -- Source Manager, Frame Distributor, Runtime Supervisor, Desired State Synchronization, Telemetry, AI Runtime package reorganization, Media Publisher, all 7 steps hardware-validated and merged to `develop`, 2026-07-29 -- all complete on `develop`) | `feature/deepstream` | Shared contracts, Threat engine, Calibration, Incident service |
| Threat engine (rule-based scoring) | `services/threat_engine/` | **Done** (merged to `develop`) | `feature/threat-engine` | Shared contracts |
| Incident service (also owns the Alarm Service — see RM-10) | `services/incident_service/` | **Done** (RM-07 and RM-10 merged to `develop`) | `feature/incident-service` | Threat engine, API service |
| Recording / evidence service | `services/recording/` | **In Progress** (RM-08 done and merged to `develop`) | `feature/recording` | Incident service |
| Camera calibration service | `services/calibration/` | **In Progress** (RM-05 done and merged to `develop`) | `feature/calibration` | Shared contracts, API service (persistence) |
| Frontend (Command Center UI) | `frontend/` | **Done** (merged to `develop`, 2026-07-26 — full RM-13 history preserved via `git subtree`; formerly the external `radar-eye-command` repository, no longer a separate development target) | `feature/frontend-integration` | API service |
| Deployment bundle / systemd | `deployments/`, `scripts/` | Not Started | `feature/deployment` | All backend subsystems |
| Developer infrastructure (formatting, linting, static analysis, dependency management, pre-commit, CI/CD, coverage tooling, developer workflow) | (repo-wide) | **Done** (merged to `develop`) | `feature/developer-infrastructure` | — |
| Testing (validation, regression testing, benchmarking, soak testing, evaluation) | (repo-wide) | Not Started | `feature/testing` | — |

Status values: `Not Started` · `In Progress` · `Blocked` · `In Review` · `Done`.

No `Owner` column is maintained here — no ratified per-person/per-agent ownership roster currently exists in `AGENTS.md` or elsewhere. If one is adopted, add it back here rather than inventing handles.

---

## Blocked

Subsystem-level blockers only.

| Blocks | Reason |
|---|---|
| RM-11 (DeepStream AI Pipeline) sizing | Jetson compute budget for 20 cameras on one Jetson has never been measured on real hardware. Recommend an early partial-capacity spike before committing to the full integration timeline (see `docs/IMPLEMENTATION_ROADMAP.md`, RM-11 risk note). |
| RM-14 (Jetson Deployment) | DeepStream / JetPack / CUDA version combination actually flashed on target hardware has not been confirmed against NVIDIA's support matrix. |
| RM-08 (Recording & Evidence Service) | Not blocked, but carries a caveat: ADR-017's continuous 30-day retention policy has never been validated against actual available storage. RM-08 implements ADR-017 as documented, with retention duration as an isolated, configurable value specifically so this can be revisited without re-architecting the service if a future storage-sizing benchmark shows it infeasible. |

---

## Frontend Audit Matrix (2026-07-29)

Per-nav-item classification requested alongside the Camera Management operator workflow (Camera Runtime v1 validation, Issue 4). Confirmed by reading each route's source (real query/mutation hooks vs. stub data) — not inferred from memory. Most of the frontend is already Production Ready; the one prior assumption this audit corrected is Tactical Map, which turned out to already be a real, honest implementation rather than needing a "Coming Soon" state.

| Nav Item | Route | Classification | Notes |
|---|---|---|---|
| Live Monitoring | `/` | Production Ready | Real query/WS-driven hooks, no mock data. |
| Tactical Map | `/map` | Production Ready | Already groups cameras by real `location` text; deliberately does not fabricate map coordinates (an earlier design review's own instruction). Initially assumed to need a "Coming Soon" state — it does not; correcting that assumption here rather than silently regressing a working feature. |
| Cameras | `/cameras` | Production Ready | Rebuilt this session — full register/edit/delete workflow, no curl required (see Recently Completed entry below). |
| Incidents | `/incidents` | Production Ready | Real hooks, no mock data. |
| Threat Review | `/reviews` | Production Ready | Real hooks, no mock data. |
| Calibration | `/calibration` | Production Ready | Real hooks, no mock data. |
| Evidence | `/evidence` | Production Ready | Real hooks, no mock data. |
| AI Analytics | `/analytics` | Production Ready | Real hooks, no mock data. |
| System Health | `/health` | Production Ready | Real hooks, no mock data. |
| Configuration — Roles/Recording tabs | `/settings` | Production Ready | Roles tab is real (admin-gated); Recording Policy is a deliberate fixed read-only display per CLAUDE.md's Recording Rules (Issue 3 — left untouched). |
| Configuration — AI Model / Notifications / Audit Log tabs | `/settings` | Blocked by future milestone | No backing endpoint exists yet (`docs/OPEN_QUESTIONS.md` Q-014; `GET /audit-log` not yet specified). Already shown via the established `DisabledFeaturePanel` "not yet available" state, not a blank/broken screen (Issue 2 — confirmed already satisfied, no change needed). |

No item in this audit fell into "Needs UI work only" or "Needs backend work" — every gap found was already either fully implemented or already using the intended not-yet-available pattern.

---

## Recently Completed

| Date | Item |
|---|---|
| 2026-08-05 | **Media Architecture Reset (ADR-028), Phase 2: Live Streaming migrated as a new, independent service -- built fresh, not moved from `apps.deepstream`.** `apps/live_stream/` imports only `shared/` and `apps.api`'s repository layer (verified by AST-level import scan, not just grep) -- it never imports `apps.ingestion` or `apps.deepstream`, and neither of those import it back. It "does not know cameras exist" (per direction): `LiveStreamingRuntime` polls `camera_media_endpoints` where `subsystem="ingestion"` (a new `CameraMediaEndpointRepository.list_by_subsystem()` method, not a new class) for `MediaEndpoint` rows only -- never the camera registry, never credentials. **New shared code, both already named in the reviewed plan's "Components to introduce," not new abstractions invented this phase**: `shared/media_transport/webrtc.py` (`WebRtcBranch` -- the payloader/capsfilter/webrtcbin triple, promise-based SDP/ICE exchange, and rebuild-fresh-per-connection discipline, generalized out of `apps/deepstream/app/live_stream/branch.py`'s hardware-proven mechanics, stripped of everything DeepStream-specific: no dual-input selector, no appsrc bitstream bridging, no OSD branch) and `shared/media_transport/webrtc_signaling.py` (`WebRtcSignalingServer`, promoted near-verbatim from `signaling_server.py` -- already fully generic). Both are reusable by AI Streaming (Phase 5) later without sharing a running instance of anything (ADR-028's "shared code, not shared ownership"). Runs on its own port (8591, `configs/live_streaming.yaml`) and hasn't touched `apps.api`'s WebRTC proxy config or the old `apps.deepstream.app.live_stream` package -- deliberately: that pairing still belongs to the old, unmigrated path during this build-alongside-then-cutover window; cutover is a distinct future step. **Two real bugs found and fixed while wiring this in, both against real hardware, not synthetic tests**: (1) Live Streaming's subscriber bin (`rtspsrc` against Camera Ingestion's republished endpoint) reconnect-looped indefinitely with `streaming stopped, reason not-linked` the moment it was built, because nothing links to its output until a browser eventually calls `handle_offer()` -- a live GStreamer source pushing into a permanently-unlinked pad is fatal. Fixed by giving each subscription a permanent split tee with a leaky-queue/fakesink drain branch (built and linked before the pipeline ever goes PLAYING, so the source is always satisfied), with `WebRtcBranch`'s per-browser-connection payloader requesting its own tee branch dynamically once a browser actually connects. A related, second-order GStreamer behavior surfaced writing this: a tee request pad that gets activated (via the pipeline's own NULL->PLAYING walk) while still unlinked never pushes a single buffer once linked later, even though the link itself succeeds structurally -- confirmed via a from-scratch minimal reproduction, independent of this codebase. Requesting a tee branch only at the moment it's used (after the pipeline is already PLAYING) avoids it; `WebRtcBranch` was generalized to track the specific pad it linked to (via the payloader's own resulting peer, not an assumed `get_static_pad("src")`) so it can release a tee request pad on teardown without needing to know upstream's shape in advance. (2) Incidentally found and fixed a latent, zero-test-coverage bug from Phase 1b: `apps/ingestion/app/config.py`'s `REPO_ROOT` was computed one `parents[]` index too shallow, so `configs/ingestion.yaml` was never actually found and `load_settings()` silently fell back to hardcoded defaults the whole time -- undetected only because the YAML's values happened to match those defaults. Fixed (`parents[3]`, matching the file's real depth) with new regression tests for both `apps/ingestion` and `apps/live_stream`'s config loaders. **Hardware-validated against the real camera**: exactly one real RTSP connection held throughout (confirmed via `ss -tnp` against the physical camera's IP, unchanged across every test below); `camera_media_endpoints`/`camera_subsystem_health` show Live Streaming subscribing only to Camera Ingestion's local endpoint, never the camera; `GST_DEBUG` confirmed real 1080p H.264 NAL units flowing from the camera through Camera Ingestion, the drain tee, and into the RTP payloader, with GStreamer's own webrtcbin confirming real RTP transmission (`on_first_rtp_packet`). **Connection-establishment latency**, measured with a real `aiortc` WebRTC client against the live signaling endpoint over 16 consecutive reconnects, zero failures: offer-to-answer avg 249ms / P95 279ms / max 279ms; offer-to-ICE-connected avg 273ms / P95 303ms / max 303ms -- sub-second, consistent. **Isolation tests, all against real running processes and the real camera**: kill Live Streaming -> Camera Ingestion survives, one RTSP session unchanged, health independent; restart Live Streaming -> re-subscribes automatically (own roster-sync loop), browser reconnects (~400ms), same underlying RTSP session to the camera the whole time (unchanged local port). Kill Camera Ingestion **gracefully** (`SIGTERM`) -> its own shutdown path deletes its `camera_media_endpoints` row, Live Streaming's roster-sync loop correctly removes the now-gone subscription; restarting Camera Ingestion -> Live Streaming's same roster-sync loop picks the republished endpoint back up automatically, both healthy again within seconds, no manual intervention. **One acceptance criterion did not pass and is an open gap, not glossed over**: killing Camera Ingestion **abruptly** (`SIGKILL`, simulating a crash rather than a clean stop) left Live Streaming's local `rtspsrc` connection sitting in a `CLOSE-WAIT` socket state for several minutes with no bus error ever posted and no reconnect attempt triggered, even after adding an explicit `rtspsrc` `tcp-timeout` (5s) to `shared/media_transport/rtsp.py`'s `build_rtsp_source_element()` -- that property bounds initial connection setup, not an established connection whose peer vanished, so it didn't help in this case (kept anyway, since it's a real improvement for the initial-connect case, verified against quality gates and the existing test suite). Needs follow-up: likely an application-level liveness check (e.g. Live Streaming cross-checking Camera Ingestion's own `camera_subsystem_health.updated_at` staleness) rather than relying solely on GStreamer's TCP-level failure detection for the crash case specifically -- the graceful-shutdown case is unaffected and already works correctly. **Also not fully verified in this environment**: full glass-to-glass decoded-frame delivery to a WebRTC client. A real `aiortc` client reliably completes ICE and, on multiple runs, GStreamer confirmed real RTP transmission -- but frame decode was intermittent across runs, traced (via `GST_DEBUG=dtlsconnection:6` and codec-negotiation inspection) to two separate, environment-specific issues in this multi-homed dev sandbox (Tailscale/Docker/IPv6-link-local interfaces all present as ICE candidates): an intermittent DTLS handshake BIO/syscall error, and non-deterministic VP8-vs-H264 codec selection by `webrtcbin`'s `create-answer` against `aiortc`'s default offer (same behavior a real browser's default offer would also present, since the frontend doesn't restrict codecs either -- not verified against a real browser in this session, since none is available in this environment). Neither reproduced as something `WebRtcBranch` controls, and this exact webrtcbin transport code is otherwise an unmodified port of the mechanics this session's own earlier hardware validation already proved work with a real browser. Full quality gates green (black/ruff/mypy clean); 104 passed across `tests/shared`, `apps/live_stream/tests`, `apps/ingestion/tests` (new: `TestSplitPad`-style cross-bin-link regression coverage in `tests/shared/test_webrtc.py`, `apps/ingestion/tests/test_config.py`). Consumers continue migrating to Camera Ingestion one at a time, per explicit direction -- Recording is next (Phase 3), never simultaneously with another. |
|---|---|
| 2026-08-04 | **Media Architecture Reset (ADR-028), Phase 1b correction: Camera Ingestion's source bin gained the `tee` ("Encoded Split") its own scope definition already required, and a real cross-bin-linking defect this surfaced was found and fixed.** Requested as a direct scope audit ("does Camera Ingestion own a decoder? it should only contain rtspsrc/rtph264depay/h264parse/tee") -- confirmed no decoder/streammux/inference/webrtcbin/encoder anywhere (import grep, docstring mentions only), but the `tee` itself was missing: `build_source_bin()` exposed a single ghost pad targeting `h264parse` directly, sufficient for exactly one consumer but not the "Encoded Split" every future local consumer (Live Streaming, Recording, ...) is meant to tap independently. Added a `tee` element (`apps/ingestion/app/source.py`, named via `tee_element_name()`, matching `apps/deepstream/app/pipeline/frame_distributor.py`'s established tee-by-name convention) between `h264parse` and the bin's boundary. **Real defect found while wiring this in**: linking a tee's request pad (an element inside the per-camera child `Gst.Bin`) directly to an element added to the parent `Gst.Pipeline` (the RTSP publisher's capsfilter) fails -- GStreamer requires pads to share a bin scope to link; crossing a bin boundary needs a ghost pad, exactly why the single-consumer version used one. First attempt (raw `tee.get_request_pad()` handed straight to the publisher) passed every unit test but failed immediately against the real camera (`not-linked` stream error, reconnect-looping) -- the gap was a missing test, not just missing code: none of the original unit tests exercised linking a returned pad to an element outside the bin. Fixed with `request_split_pad()`/`release_split_pad()` (`source.py`): each call requests a fresh tee pad and ghosts it out to the bin's boundary (`Gst.GhostPad.new` + `bin_.add_pad` + `set_active(True)`, since the bin is already PLAYING when a camera is added), so any number of independent local consumers can each get their own working cross-bin pad; released symmetrically (unghost + `tee.release_request_pad`) during teardown/reconnect, after `unpublish()` and before the bin itself is NULLed, matching this codebase's sink-to-source teardown discipline. New regression tests added that specifically link a requested split pad to an element in a *separate* pipeline (`TestSplitPad`, `apps/ingestion/tests/test_source.py`) -- the exact scenario the original suite missed -- plus a two-simultaneous-consumers test. First-buffer health probe moved from the bin's (now-removed) ghost pad to the tee's own sink pad, so the CONNECTED signal stays correct regardless of how many downstream branches exist. **Re-validated end to end on real hardware**: camera re-registered against a freshly-migrated DB, Camera Ingestion held exactly one RTSP connection to the physical camera (`192.168.68.10:554`, confirmed via `ss -tnp`), a real `gst-launch-1.0` subscriber received live RTP data through the new tee/ghost path (1300+ packets, ~887kbps), `camera_subsystem_health` showed `CONNECTED`, clean shutdown left no RTSP connection behind. 96 passed in `apps/ingestion/tests` + `tests/shared` (up from 93 -- 3 new `TestSplitPad` tests); black/ruff/mypy clean. |
| 2026-08-04 | **Media Architecture Reset (ADR-028), Phase 0 + Phase 1b: Camera Ingestion Service built as a new, standalone subsystem.** Root cause behind this reset: a hardware-measured pipeline trace proved DeepStream owning camera ingestion inside its own `Gst.Pipeline` is a reproducible production defect -- `nvinfer`'s synchronous TensorRT engine deserialization during the shared pipeline's state-change walk blocked `rtspsrc`/`depay` (a topologically unrelated branch) from advancing, making live video hostage to AI model-loading time (measured: 9.18s). **Phase 0** (docs only): new ADR-028 recording the decision; `docs/DEEPSTREAM_PIPELINE_SPEC.md` rewritten (Camera Ingestion is no longer "part of the DeepStream pipeline"; new Stage 1.5 Live Streaming; Stage 2/5.5/9 updated); `docs/CAMERA_RUNTIME_LIFECYCLE.md` §7's superseded direction marked, not deleted; `docs/FRONTEND_BACKEND_CONTRACTS.md` gained its missing WebRTC section; `docs/OPEN_QUESTIONS.md` Q-007 closed. **Phase 1b** (code): `apps/ingestion/` -- a completely new process, migrating no existing code, wired to zero consumers -- owns exactly one real RTSP connection per camera (a hard, user-confirmed requirement: this camera's low concurrent-RTSP-session tolerance was independently discovered this session), publishing it locally via a new **Media Distribution Interface** (`shared/media_transport/`: `MediaEndpoint`/`build_source_element()`, transport-agnostic by design -- RTSP is the Phase 1 implementation, not the architectural contract; shared memory/SRT/QUIC/etc. can replace it later without any consumer change). Camera Ingestion is a strict platform service (checked via import grep, not just docstring): zero imports of `apps.deepstream`, zero DeepStream-specific GStreamer elements, zero WebRTC code -- "I receive video. I distribute video," nothing else. Reused this session's own Observed-State-accuracy discipline (RECONNECTING until a real first buffer confirms CONNECTED). Two new cross-process tables (`camera_media_endpoints`, `camera_subsystem_health`, migration `5e336d6ed50e`) extend the existing Desired/Observed State Postgres-sharing pattern to the new multi-process world. `AsyncBridge`/`ReconnectPolicy` promoted to `shared/` (zero behavior change, just correct ownership -- every future GStreamer-owning process needs them, not just DeepStream). **Real hardware defect found and fixed during verification**: `h264parse`'s output `stream-format` is negotiated, not fixed -- left unconstrained it silently defaults to `avc` (length-prefixed NALs), which `rtph264pay`'s `config-interval=-1` in-band SPS/PPS re-insertion needs `byte-stream` to work at all; produced an RTP stream that looked correct (right payload type, right SDP) but never actually decoded downstream. Fixed with an explicit capsfilter forcing `byte-stream`/`alignment=au` between the published pad and the payloader. **Hardware-validated end to end**: the real camera (previously found to have a low concurrent-RTSP-session tolerance) held one real connection while a real subscriber (`gst-launch-1.0`) independently connected to the republished local endpoint and received live RTP data (600+ packets, real bitrate) -- confirming the one-upstream-connection design directly solves the session-limit problem discovered earlier this session, not just architecturally but operationally. `run.py` gained a new, independently health-checked `ManagedService` entry (port 8600), started/stopped first/last per the new architecture's dependency shape, still fully optional -- nothing else in the stack references it yet. Consumers migrate to it one at a time starting with Live Streaming (Phase 2), never simultaneously, per explicit direction. Full quality gates green (black/ruff/mypy clean); 603 passed across `apps/ingestion/tests`, `tests/shared`, `apps/api/tests`, `apps/deepstream/tests` (4 pre-existing environmental deselects, unrelated -- `.env`-dependent `test_config.py` cases, `gi`-binding-dependent `test_bridge.py`). |
| 2026-08-04 | **Three stacked, hardware-confirmed fixes that took Live Monitoring from "freezes/hangs on repeated delete-re-register and reconnect" to "streams reliably, with Camera Management's Connection Status honestly reflecting reality."** Each is its own commit. **1. Synchronizer-freeze fix**: `RuntimeSupervisor._run_worker`'s `except asyncio.CancelledError` handler called `command.future.cancel()` on a future a *different* task (`DesiredStateSynchronizer`'s own polling loop) was awaiting -- `CancelledError` is a `BaseException` that slipped past that task's `except Exception` and was silently swallowed by its own outer `except asyncio.CancelledError: pass`, permanently killing the synchronizer with no traceback. Fixed in `runtime_supervisor.py` by replacing `.cancel()` with `.set_exception(RuntimeSupervisorError(...))` in three places (`_run_worker`'s handler, `remove_camera()`'s queue-drain, `stop()`'s queue-drain) -- the awaiting caller now gets a normal exception instead of having its own task cancelled out from under it; two tests updated to assert the new exception type. **2. WebRTC ICE-restart fix**: a persistent, reused `webrtcbin` (built once in `build()`) was hardware-confirmed to never properly restart ICE on a second browser connection to the same camera -- client-side `iceConnectionState` permanently stuck at "checking", 100% reproducible from the second connection onward (e.g. a page refresh). Fixed in `live_stream/branch.py` by rebuilding payloader/webrtc_caps/webrtcbin fresh on *every* `handle_offer()` call (`_build_webrtc_transport`/`_teardown_webrtc_transport`), reusing the same IDLE-probe quiescing pattern as `DeepStreamPipeline.remove_source()` so this doesn't reintroduce the already-fixed sink-to-source freeze -- every connection is now structurally identical to the always-reliable first one. **3. Observed-State-accuracy fix** (the one that made this session's "Camera Management shows Connected/healthy but Live Monitoring is a black screen" report possible to actually diagnose): `DesiredStateSynchronizer`'s `on_source_connected` hook, and the reconnect path in `runtime.py`, both marked a camera `CONNECTED` the instant its GStreamer source bin was successfully constructed and linked -- which says nothing about whether `rtspsrc` had actually finished its RTSP handshake with the camera, so a camera could sit "Connected"/healthy in the UI indefinitely while never receiving a single real frame. Fixed: a camera is now marked `RECONNECTING` (not `CONNECTED`) the moment its source is added -- `RuntimeAdapter.on_camera_reconnecting` gained a `reconnect: bool = True` parameter so this fresh-add path can pass `reconnect=False` and not falsely increment `reconnect_count` for a camera that has never actually reconnected from anything -- and is only promoted to `CONNECTED` by a new one-shot pad probe (`DeepStreamPipeline._attach_first_buffer_probe`, `pipeline/builder.py`) on the bitstream tee's sink pad, fed directly by `h264parse` and independent of decode/AI/valve state -- the earliest point that actually confirms real data is flowing. The same probe mechanism covers both the initial add and every reconnect, since both funnel through the single `add_source()` choke point. 4 new unit tests (real GStreamer, no mocks, matching this module's established convention): the probe fires exactly once on a real buffer, is a no-op with no callback or no tee, and `reconnect=False` leaves `reconnect_count` untouched. **Hardware-validated end to end**: a freshly registered camera now shows real video in Live Monitoring (raw passthrough, AI off) reliably, and Camera Management's Connection Status/Health now honestly reflect the pipeline's actual state instead of optimistically claiming success. Separately confirmed via live terminal logs (Production Validation Mode): with AI still disabled, the camera's raw bitstream visibly flows through the DeepStream process's own `live_stream` transport stage (`Bitstream received` -> `AppSrc push` -> `RTP packet transmitted`, once per real RTSP access unit) -- expected and by design, since Live Streaming's passthrough taps the bitstream tee *before* the decoder/AI valve (`docs/DEEPSTREAM_PIPELINE_SPEC.md`'s "every media representation exists exactly once" principle); no PGIE/tracker/SGIE activity accompanies it. Full `apps/deepstream` test suite green (346 passed, same 2 pre-existing environmental failures -- `test_default_mainloop_factory_requires_gi`, `test_camera_without_stream_profile_is_skipped`, both confirmed present on a clean checkout, unrelated to any of these changes); black/ruff/mypy clean. |
| 2026-08-03 | **Live Monitoring dynamic AI switching (Stage C), the camera re-registration bug (root-caused and fixed), and complete removal of Lifecycle from the product.** Three pieces of one arc, each its own commit. **Stage C**: `live_stream/branch.py` gained a second, annotated input chain (SGIE tee -> queue -> OSD -> `nvv4l2h264enc` -> `h264parse`) feeding the existing `input-selector` alongside the already-validated raw passthrough input; `select_input(StreamInput)` switches the selector's `active-pad` synchronously on the GLib thread and forces a keyframe on the annotated encoder via `GstVideo.video_event_new_upstream_force_key_unit` when switching to it. `runtime.py`'s `on_valve_changed` hook translates `ai_enabled` -> `StreamInput` -- `live_stream/` itself never says "AI". A real hardware defect was found and fixed within scope: `nvv4l2h264enc` defaulted to Constrained-Baseline profile while the camera's own bitstream is Main profile, causing an expensive mid-stream decoder reinit on every switch; fixed with `encoder.set_property("profile", 2)`. Hardware-validated in both directions (AI OFF->ON and ON->OFF) with structured switch-latency logging, zero reconnects/renegotiations across repeated cycles. **Camera re-registration bug**: deleting and re-registering the same physical camera left it permanently stuck Reconnecting/Disconnected. Root cause: `RuntimeAdapter.on_camera_connected` (the only code path that persists Observed State as `CONNECTED`) was called only from `runtime.py`'s startup batch and the bus-error/EOS reconnect path -- never from `DesiredStateSynchronizer`'s ongoing convergence loop, which is what actually adds a camera registered after process startup. Fixed by adding an optional `on_source_connected` hook to `DesiredStateSynchronizer`, fired once per real `add_source`, wired to `RuntimeAdapter.on_camera_connected`; 4 new unit tests plus repeated hardware delete/re-register cycles confirmed the fix. **Lifecycle removal**: per explicit final architectural decision, `lifecycle_state` (`DRAFT/TESTING/VERIFIED/OPERATIONAL/MAINTENANCE/DISABLED`) is gone from the product -- model column and check constraint (migration `b4d6e8f0a1c2`, dropping the column added by `7a2c4e9d1b3f`), `CameraSchema`/`CameraLifecycleUpdateRequestSchema`, the `PATCH /cameras/{id}/lifecycle` route, `CameraRegistryService.transition_lifecycle()`/`LIFECYCLE_TRANSITIONS`, `CameraLifecycleChangedPayload`/`Event`, `synchronization.py`'s `LifecycleSourcePolicy`/`DefaultLifecycleSourcePolicy` (a registered camera now always wants an active source; AI eligibility is `ai_enabled` alone), and the entire frontend Lifecycle State column/transition UI. There are now exactly two operator controls per camera: registration/deletion (implicit connect/disconnect) and AI Enable/Disable. **A second real hardware defect was found and fixed during this removal's own repeated delete/re-register hardware validation**: `CameraWebRtcBranch.teardown()` (`live_stream/branch.py`) NULLed its GStreamer elements in construction (upstream-to-source-to-sink) order; NULLing the queue immediately after the SGIE tee while its downstream neighbors (OSD, encoder, `webrtcbin`) were still PLAYING deadlocked a streaming thread mid-push, permanently wedging the single GLib main-loop thread that services every pipeline mutation (`AsyncBridge.schedule_on_mainloop`) -- freezing add/remove/enable/disable for every camera, not just the one being deleted, with no exception raised (confirmed via a temporary `faulthandler` all-threads stack dump, since `py-spy`/`gdb` both require `ptrace` permissions unavailable in this environment). Fixed by tearing elements down sink-to-source (`reversed(self._elements)`), the documented GStreamer convention for removing elements from a running pipeline. Re-validated after the fix: 8 consecutive delete/re-register + AI-enable/disable cycles against real hardware with zero restarts, zero hangs, zero tracebacks (excluding the separate, pre-existing, already-documented Tier2/`nvstreamdemux` pad-exhaustion warning), GPU memory flat (954-970MB) across the whole run, clean graceful shutdown (`SIGTERM`) confirmed working again post-fix. Full quality gates green: 726 passed / 4 pre-existing environmental failures (`.env`-dependent `test_config.py`, `gi`-binding-dependent `test_bridge.py`, unchanged), ruff/black/mypy clean; frontend `eslint`/`vite build` clean. |
| 2026-07-30 | **Camera Connectivity decoupled from Lifecycle/AI Runtime ownership -- a minimal, targeted fix, not a Camera Runtime redesign.** Operator Acceptance Testing's next finding after Camera Management: a registered camera stayed `DRAFT`/`DISCONNECTED`/`OFFLINE` forever. Root-cause investigation (three parallel Explore agents tracing the DeepStream coupling, the frontend Live Monitoring data path, and the architecture docs/ADRs/DB schema) found `DesiredStateSynchronizer`'s `DefaultLifecycleSourcePolicy.should_have_active_source()` only gave a camera a source when `lifecycle_state == "OPERATIONAL"` -- three explicit lifecycle promotions away from registration. `RuntimeSupervisor.enable_ai`/`disable_ai` already had zero lifecycle awareness; the only coupling point was this one policy method plus the implicit `if is_active:` guard around AI convergence. No ADR or committed design doc mandated the OPERATIONAL-only gating -- `DefaultLifecycleSourcePolicy`'s own docstring disclosed it as "a deliberate, conservative reading," an implementation choice, not a specified requirement. Planning went through three iterations before landing: an initial "migration-only" plan, then a `ConnectivityManager` ownership-layer proposal (explicitly rejected -- "Do not introduce new architectural layers... the smallest correct change wins"), settling on the minimal fix actually implemented. **Change**: `DefaultLifecycleSourcePolicy` (`apps/deepstream/app/synchronization.py`) gained a second method alongside the existing one -- `should_have_active_source` now returns `lifecycle_state != "DISABLED"` (was `== "OPERATIONAL"`); new `should_allow_ai` returns `lifecycle_state == "OPERATIONAL"`, preserving today's AI-eligibility behavior exactly. `_converge_one()`'s AI block now checks `ai_enabled AND should_allow_ai(...)` explicitly, since connectivity no longer implies OPERATIONAL. `RuntimeSupervisor`/`DesiredStateSynchronizer`'s `_ensure_source`/`_remove_source`/`MediaPublisher`/`DeepStreamPipeline` -- all unchanged, per explicit direction to preserve Camera Runtime v1's architecture and minimize code movement. **Observed State persistence** (a second, independent gap the same investigation found): nothing in the codebase ever wrote `cameras.status`/fps/latency/etc. to Postgres -- `RuntimeAdapter` and `HealthCollector` were both purely in-memory, in two separate OS processes that don't share memory, so `GET /health/cameras` always returned nulls regardless of the lifecycle fix. New migration `a3c4d5e6f7b8` adds `fps`, `latency_ms`, `last_seen_at`, `reconnect_count`, `last_stream_error` to `cameras`. No new component: `RuntimeAdapter.on_camera_connected`/`on_camera_reconnecting`/`on_camera_disconnected` each gained a few inline lines writing directly to the repository (event-driven, one write per transition, immediate); `HeartbeatScheduler` gained an `on_health_snapshot` hook (keeping it decoupled from apps.api/the database, matching its own established design) that `runtime.py` throttles to a new `observed_state_flush_interval_seconds` setting (default 3s) before persisting fps/latency -- explicit write-volume constraint from the approved plan: high-frequency metrics must never be written per-frame or even every heartbeat tick. `GET /health/cameras`/`GET /cameras/{id}/health` (`apps/api/app/routers/health.py`) switched from reading `HealthCollector`'s dead in-memory camera-heartbeat dict to the new DB columns via `CameraRepository`; `CameraHealthSchema` gained `latency_ms`/`reconnect_count`/`last_stream_error`. No frontend changes needed -- Live Monitoring and the Cameras page already trust `GET /cameras`/`GET /health/cameras` verbatim with no client-side fabrication, confirmed during investigation. **Hardware validation found and fixed one real, migration-caused bug**: `runtime.py`'s `_schedule_reconnect` assumed every camera's `ReconnectPolicy` was pre-populated in `self._policies` at `start()` -- true before this fix (only OPERATIONAL cameras, typically already active at startup, ever needed one) but now routinely false (any camera added after startup, which is now the common case, hits a `KeyError` crash on its first connection failure). Fixed with `self._policies.setdefault(...)`, lazily creating the policy on first use -- the same construction `start()` already did, just no longer requiring pre-population. **Hardware validation also found, and explicitly did not fix, a second, pre-existing, orthogonal defect** (out of scope -- "do not redesign," this needs its own investigation): `_schedule_reconnect`'s GLib-timeout retry loop has no exit condition tied to Desired State at all -- once a reconnect chain is in flight, transitioning the camera to `DISABLED` does not stop it. Discovered when a real Hikvision camera's stale credentials caused a reconnect storm (>4000 attempts against real hardware in minutes) that continued climbing even after the camera was moved to DISABLED; the deepstream process was stopped manually to halt it, since the API offered no way to. Logged as a known gap in `docs/CAMERA_RUNTIME_LIFECYCLE.md` §6, not bundled into this fix. Separately: this session's own `apps/deepstream/tests/conftest.py`/`apps/api/tests/conftest.py` `db_engine` fixtures drop and recreate every table against the same physical dev database used for manual/hardware testing (a pre-existing sandbox characteristic, not new) -- running the test suite mid-implementation wiped a camera the operator had registered live during their own Operator Acceptance Testing session; schema and the `testadmin` account were restored immediately, disclosed to the operator, but the registered camera data itself was not recoverable. Verified: full `apps/deepstream/tests`+`apps/api/tests` suite green (478 passed, same 4 pre-existing environmental failures), ruff/black/mypy clean; hardware validation against a real DeepStream runtime + real camera confirmed the core fix (a DRAFT camera connects and gets real `last_stream_error` persisted, without any lifecycle promotion) and confirmed AI stays off at DRAFT; a full successful-connection promotion/AI-toggle cycle could not be completed against real hardware in this environment because no known-working camera credentials were available and continuing to retry risked further hammering real infrastructure via the disclosed reconnect-loop gap above -- the equivalent behavior (promotion doesn't recreate the source, AI toggles correctly, MAINTENANCE/DISABLED transitions) is covered deterministically by the updated unit test suite instead. |
| 2026-07-29 | **Camera Management operator workflow implemented -- the Cameras page is now the complete Camera Management interface; no curl/SQL/terminal required.** Camera Runtime v1 validation surfaced that adding/editing/removing a camera had no UI path at all, only `POST /cameras` via curl -- an explicit, mandatory gap to close. Backend: `apps.api.app.services.rtsp_url_generator` (new) -- one `RtspUrlAdapter` per brand (Hikvision/Dahua/Uniview/Axis/Hanwha), extensible by adding one adapter entry, never touching callers; the operator supplies brand+IP+credentials+port/stream, the backend builds and stores only the generated, encrypted RTSP URL, never accepting one directly. `CameraRegistryService` gained `update_connection()` (merges whichever connection fields are given, regenerates+re-encrypts the URL, preserves the existing password when omitted -- write-only, never returned) and `delete()` (removes a camera's own setup data; leaves incidents/review items/recordings alone via the DB's own FK constraint, translated to a 409 by the router -- Evidence Preservation, CLAUDE.md). New routes: `POST /cameras`, `PATCH /cameras/{id}` (extended with brand/model/ip_address/port/username/password/transport/stream_path), `DELETE /cameras/{id}`, `GET /cameras/brands`. Migrations `e1a2b3c4d5f6` (connection fields) and `f2b3c4d5e6a7` (a `model` field -- found missing against the operator's own field list mid-implementation and added the same way, not deferred). Frontend: `frontend/src/routes/cameras.tsx` rebuilt -- an 11-column table (Camera Name/Brand/IP/Location/Lifecycle State/AI Enabled/Recording Enabled/Connection Status/Health/Last Seen/Actions), an Add/Edit Camera form (react-hook-form + zod, brand-driven port/stream placeholder defaults from `GET /cameras/brands`, password field never pre-filled and omitted from the PATCH body entirely when left blank so it's never cleared by accident), a lifecycle-transition control (mirrors the backend's `LIFECYCLE_TRANSITIONS` state machine client-side as a UX convenience only -- the backend remains the sole enforcer, a stale copy here can only produce an over-permissive *offer* that still gets a 422, never a wrongly-*accepted* transition), and a delete confirmation dialog. New hooks: `useCreateCamera`/`useDeleteCamera`/`useUpdateCameraLifecycle` (`features/cameras/hooks/useCameraMutations.ts`) and `useCameraBrands` (new file, feature-scoped since only this form needs it, per the existing `queries/` vs `features/*/hooks/` promotion convention). Confirmed already-satisfied without further work, not newly built (see the Frontend Audit Matrix above for the full per-page record): Settings' AI Model/Notifications/Audit Log tabs already show `DisabledFeaturePanel`'s professional "not yet available" state; Recording Policy is already a fixed read-only display; Tactical Map is already a real, honest implementation, not a stub as initially assumed. Verified: 27 camera-specific backend tests plus the full 156-passing repo suite (same pre-existing environmental `test_config.py` failures, unrelated), ruff/black/mypy clean; a live end-to-end curl cycle against a running instance (register with model field -> edit IP+model -> lifecycle DRAFT->TESTING -> delete, each step inspected) confirmed the whole chain outside the test suite too; frontend `tsc --noEmit` and `eslint` both clean. Browser click-through of the Add/Edit/Delete dialogs was not performed -- no browser automation tool is available in this environment; verification instead relied on typecheck/lint plus the live backend cycle above, disclosed here rather than assumed. |
| 2026-07-29 | **First manual validation failure found and permanently fixed: `run.py` now guarantees the dev administrator account.** First end-to-end login attempt (`testadmin`/`TestPass123!`) failed with 401. Root-caused, not worked around: this sandbox's PostgreSQL data does not reliably persist across a reset boundary that recurred repeatedly over this project's development (`alembic upgrade head` has had to run from empty multiple times) -- one such reset wiped the `users` table, deleting the account `scripts/create_test_user.py` had created earlier. The authentication code itself (`LocalUserAuthProvider` → `UserRepository` → bcrypt → JWT) was verified correct end-to-end by direct inspection; it fails only because the row is genuinely absent. Fixed by adding `ensure_test_user()` (create-if-missing only, never touches an existing password/role) to `scripts/create_test_user.py`, called by `run.py` on every startup after migrations -- prints "Development administrator created" or "verified". Hardware-verified: fresh run creates + login succeeds; second run reports "verified" with zero duplicate rows; a password changed directly in the database survives a further launcher run untouched. New `docs/VALIDATION_LOG.md` (empty template plus this first closed entry) records the finding. Commit `cd72732`. |
| 2026-07-29 | **Project consolidation: `origin/develop` synchronized; one-command launcher (`run.py`) implemented and hardware-validated.** A git/GitHub audit (current branch, `develop` vs `origin/develop`, which branches contain Camera Runtime v1) found local `develop` was 19 commits ahead of `origin/develop` — every Camera Runtime v1 commit (Camera Registry through Production Validation Mode) had been made directly on local `develop` but never pushed. No divergence, no conflicts (`origin/develop` was a strict subset, purely a fast-forward) — confirmed via `git log develop..origin/develop` (empty) — so no merge decision was required; `git push origin develop` fast-forwarded `origin/develop` from `e0a7f79` to `30a517e`, verified identical afterward (`git rev-parse develop origin/develop`). No other branch (`feature/deepstream`, `feature/api`, `master`, etc.) contains any Camera Runtime v1 commit independently. Separately, `run.py` (repo root, stdlib-only, no new dependency) now brings up the complete system with one command: re-execs itself into `.venv` if not already running there, runs read-only PASS/FAIL prerequisite checks (Python, venv, dependencies, Docker [informational — unused by this project], `.env`, configuration, PostgreSQL reachability, Node/Bun, NVIDIA/GStreamer/DeepStream plugins, required model files, ports 8000/8080), runs `alembic upgrade head`, then starts Backend API → DeepStream Runtime → Frontend in order, waiting for each to report healthy before continuing (API via `GET /health/system` polling, DeepStream/Frontend via their own startup log lines) — the exact same startup commands documented in `docs/OPERATOR_RUN_SHEET.md`, run as subprocesses, no application code duplicated or reimplemented. `--validation` temporarily flips `configs/validation.yaml`'s `production_validation_mode.enabled` to `true` before starting DeepStream and always restores the original file on shutdown (including on crash). Hardware-validated end-to-end, including the two behaviors that can't be verified by reading the code: (1) a full `--validation` run — all three services reached healthy, the dashboard and watchdog rendered correctly, the closing summary matched the requested format, Ctrl+C (SIGINT) stopped all three in reverse order cleanly, and the validation-mode config flag was correctly restored to `false` afterward; (2) a deliberate mid-run crash (`kill -9` on the DeepStream child) was correctly detected, reported (`[FAIL] deepstream exited unexpectedly (code -9)`), and the remaining two services were stopped cleanly in response — zero orphaned processes confirmed both times. Full quality gates green (694 passed, same 4 pre-existing environmental failures; ruff/black/mypy clean on `run.py`). Commit `b32b1e9`. |
| 2026-07-29 | **Production Validation Mode implemented and hardware-validated** (`7d31840`) — a single config switch (`configs/validation.yaml`'s `production_validation_mode.enabled`, off by default) giving maximum observability for the first end-to-end manual UI acceptance run, built entirely by reusing existing RM-11.SIV instrumentation rather than adding a parallel mechanism: the existing per-frame pipeline trace (`pipeline_trace.py`, FRAME RECEIVED → ... → EVENT PUBLISHED), the existing 18 `radar_eye.stage.*` loggers (new `stage_logging.enable_maximum_observability()` elevates all to DEBUG), and the existing `Dashboard`/`Watchdog` classes (`apps/deepstream/app/siv/`, previously only ever constructed by `scripts/run_siv.py`, now also constructible from the real production entrypoint `apps.deepstream.app.main` when the flag is set). `Dashboard` output moved from stdout to stderr so it doesn't clobber structured JSON application logging sharing the same stream. Hardware-validated with a direct on/off comparison: FPS/latency identical with the mode off (byte-for-byte baseline behavior preserved); with it on, no measurable FPS regression (24.75 FPS both ways) and latency within this session's established run-to-run noise band; the dashboard correctly distinguished live subsystems from genuinely idle ones (Calibration/ThreatEngine/IncidentService/AlarmService correctly reported `STALLED` with zero real detections occurring in the test scene); clean shutdown both ways. Full quality gates green (694 passed, same 4 pre-existing environmental failures, ruff/black/mypy clean). Three new docs delivered: `docs/PRODUCTION_VALIDATION_MODE.md` (what it does, trade-offs, honest disclosure of which of the 18 stage loggers currently have real call sites vs. are declared-but-silent placeholders), `docs/PRODUCTION_VALIDATION_CHECKLIST.md` (35-item PASS/FAIL checklist per subsystem), and `docs/PRODUCTION_RUN_GUIDE.md` (the 7-section run guide — Preconditions, Terminal Commands, IDE Instructions, Runtime Expectations, Manual UI Validation Checklist, Log Locations, Shutdown Procedure — every command verified against this repository and this machine; one verified-safe Python one-liner replaced an initially-drafted `sed` command that would not actually have worked; a real pre-existing `uvicorn` process found squatting on port 8000 during verification, origin unknown, is called out as a precondition check rather than silently worked around). |
| 2026-07-29 | **Camera Runtime v1 wired into the production entrypoint; a real segfault found and permanently fixed on real hardware.** Preparing for the first end-to-end UI acceptance run surfaced that Camera Runtime v1's completed Steps 1–7 were never actually constructed by `apps.deepstream.app.main`/`runtime.py` — `DeepStreamRuntime` never imported `RuntimeSupervisor`, `DesiredStateSynchronizer`, `TelemetryCollector`, or `MediaPublisher`; `start()` still ran the pre-RM-12 unconditional "load every registered camera" path. Fixed under an explicit "integration only, no redesign" mandate: `runtime.py`'s `__init__` now constructs all four in dependency order (`RuntimeSupervisor` → `MediaPublisher` → `DesiredStateSynchronizer` → `TelemetryCollector`, resolving the `DeepStreamPipeline`↔`MediaPublisher` circular construction dependency via a new `DeepStreamPipeline.set_tier2_publisher()` setter, mirroring the existing `set_health_collector()` pattern); `start()` now calls `DesiredStateSynchronizer.synchronize()` instead of the old unconditional load; `stop()` tears down in reverse order. Hardware-validated: FPS/latency matched the established baseline (~24.7 FPS / ~5ms) with all four components live (`42b6761`). **A second, explicitly-authorized follow-up** then added a temporary polling loop (`_synchronize_desired_state_forever`, config `desired_state_sync_interval_seconds`, default 1s) since `synchronization.py`'s own docstring states Step 4 deliberately shipped without any automatic trigger — without it, Enable/Disable AI and lifecycle_state changes made via the API would update the database but never reach the running pipeline short of a process restart. Hardware-validated live (no restart): Disable AI, Enable AI, and an `OPERATIONAL → MAINTENANCE → OPERATIONAL` lifecycle transition all converged within one ~1s poll tick, with zero per-tick log spam on the (overwhelmingly common) no-op case (`42b6761`). **Stress-testing that poll loop against real hardware then reproduced a genuine, previously-latent process crash**: removing a camera's source while Visualization + Media Publisher (Tier 2/`nvstreamdemux`) were both active segfaulted the whole process inside NVIDIA's `libgstnvvideoconvert.so`, on the visualization branch's `viz-queue` streaming thread (confirmed via `journalctl -k`, reproduced twice, deterministically, on the first `remove_source()` call after startup). Root-caused via NVIDIA/GStreamer documentation and NVIDIA's own reference `deepstream-app` source (`apps-common/src/deepstream_source_bin.c`, present on this machine's DeepStream 7.0 SDK install) rather than guessed at: dynamically calling `release_request_pad()` on `nvstreammux`/`nvstreamdemux` while the pipeline is PLAYING is exactly the operation NVIDIA's own reference implementation never performs and multiple independent NVIDIA Developer Forum threads document as unsafe — pads are meant to be requested once and only ever linked/unlinked afterward. (An initial narrower fix — blocking the source bin's ghost pad with an `IDLE` probe before tearing down the decoder, guarding a real but secondary race — was tested first and confirmed, by reproducing the identical crash with that guard already in place, that pad *release* itself was the actual cause, not decoder-teardown timing alone; that guard was kept as defense in depth.) Fixed in `DeepStreamPipeline` (stable, process-lifetime `camera_id → pad_index/sink_pad` assignment, reused across any number of remove/re-add cycles; `remove_source()` now only unlinks, never releases; also sends NVIDIA's own documented `GST_NVEVENT_STREAM_RESET` event via `pyds.gst_element_send_nvevent_new_stream_reset()` before teardown) and `Tier2Publisher` (identical never-release pattern for `nvstreamdemux`'s per-camera request pad). One existing test (`test_repeated_add_remove_across_cameras_does_not_leak_names`) relied on the old release-and-reuse-the-same-index behavior across *different* camera_ids and was updated to assign distinct indices, matching real pipeline behavior; a new test explicitly covers the scenario the fix targets (the same camera_id cycling through repeated remove/re-add reuses its one demux pad). Hardware-validated after the fix: 15 consecutive full remove/re-add + AI-toggle cycles against the real camera and production entrypoint with Visualization + Media Publisher both active — zero segfaults, zero quiesce-timeout warnings, stable FPS/latency/GPU memory throughout (no leak), clean shutdown (`018318d`). Full repository quality gates green throughout (694 passed; the same 4 pre-existing, environment-conditional failures — three `.env`-dependent `test_config.py` cases, one `gi`-binding-dependent `test_bridge.py` case — confirmed present with or without these changes via `git stash`; ruff/black/mypy clean). Not yet reviewed for the still-pending 7-section Run Guide / first manual UI acceptance run. |
| 2026-07-29 | **RM-12 VMS redesign: Camera Runtime v1 complete on `develop` — all 7 blueprint steps implemented, hardware-validated, and committed; the implementation phase of the Camera Runtime effort is closed (design docs not committed to the repository — this entry, plus each step's own commit message, is the durable record).** Built component-by-component against the frozen RM-12 Camera Runtime Implementation Blueprint (an enterprise architecture redesign produced and frozen entirely in conversation across an earlier session — never committed as a standalone doc; the same disclosed gap as the Camera Registry entry below, now spanning the whole subsystem). Core architectural decisions this closes out: per-camera **valve-gating** (not dual-mux — replaced after a real-hardware feasibility study, `VALIDATED WITH LIMITATIONS`) as the sole AI enable/disable mechanism; the **Two-Tier Frame Distribution Pattern** (Tier 1 raw media, structurally independent of AI state; Tier 2 annotated media, gated for free by the valve's position upstream of PGIE/Tracker/SGIE); Camera Registry owns Desired State, Camera Runtime owns Runtime execution/convergence, Telemetry only observes — boundaries validated as intact through to the final step. Seven steps, each its own commit, each hardware-validated on real camera(s)/models before being accepted: **Step 1 — Source Manager** (`0f5a3dd`): per-camera `valve` element added to every source bin, permanently open, pipeline behavior statistically unchanged from pre-valve baseline. **Step 2 — Frame Distributor** (`ec9c324`): per-camera Tier 1 `tee` before the valve, raw branch terminated at a stub sink (bounded/leaky queue → fakesink), AI path unaffected. **Step 3 — Runtime Supervisor** (`b0ffe81`): per-camera command queue + serialized dispatcher for `EnableAI`/`DisableAI`, idempotent state convergence, GPU admission via a pluggable `ConcurrentEnableLimiter`, every valve mutation scheduled through a new `AsyncBridge.schedule_on_mainloop` (asyncio→GLib direction). Real hardware validation caught and fixed a genuine defect before commit: a fresh camera's `ai_enabled` bookkeeping defaulted to `False` instead of unknown, so a camera's very first `DisableAI` was misread as an already-satisfied no-op and silently never closed the valve — fixed by tracking tri-state (unknown/true/false). **Step 4 — Desired State Synchronization** (`ba9f870`, lifecycle-policy extraction follow-up `50d9ea8`): `DesiredStateReader` (read-only) + `DesiredStateSynchronizer` (reconciliation, not imperative execution) connect Camera Registry's Desired State to Runtime State; lifecycle-source policy centralized behind a `LifecycleSourcePolicy` seam per review feedback, not hard-coded. **Step 5 — Telemetry** (`6eb7cff`): `TelemetryCollector` — observational-only liveness/readiness/metrics, zero write path into Camera Runtime; readiness can be false while liveness stays true (proven on real hardware via an injected transient DB-read failure). **Step 6 — AI Runtime Refactor** (`8432ed7`): `runtime_adapter.py`/`threat_runtime_adapter.py`/`observations.py` reorganized into an explicit `apps/deepstream/app/ai_runtime/` package (`detection.py`/`threat_bridge.py`/`observations.py`) — pure move, zero behavioral change, confirmed via full real-hardware re-run matching the established FPS/latency baseline exactly. **Step 7 — Media Publisher** (`cce9328`, the largest step): new `apps/deepstream/app/media_publisher/` package — `Tier1Publisher`/`Tier2Publisher` share a `ConsumerRegistry` (thread-safe, failure-isolated dispatch) and `_TieredPublisher` (attach/detach/register/unregister/shutdown lifecycle); Tier 2 required a new shared `nvstreamdemux` in `pipeline/builder.py` (SGIE's branching generalized so visualization and Tier 2 share one tee when either is active — both-disabled case remains byte-for-byte identical to pre-existing behavior). Publisher interfaces only, no transport implementation (explicitly future work). Real hardware validation (two independent camera pipelines — only one physical camera available, the second registered against the same physical RTSP stream, disclosed explicitly) confirmed every required point with measured evidence: multi-camera routing, Tier 1 continuity through an AI toggle cycle, Tier 2 correctly stopping/resuming with AI, per-camera attach/detach isolation, consumer failure isolation (74 real failures logged and isolated, healthy consumer unaffected), clean shutdown, no pad leaks, and final steady-state FPS/latency (24.72 FPS / 5.41ms) matching the Step 1 baseline (~24.7 FPS / ~5.4ms) with no measurable regression across the entire 7-step effort. Full repo suite green after every step (final: ~692-693 passed, same small set of pre-existing environmental failures throughout — three `test_config.py` cases that only reproduce in a `.env`-less environment, one `test_bridge.py` case specific to this dev machine's genuinely-working `gi` bindings, one intermittently-flaky DB-order test independently reconfirmed passing in isolation each time it appeared); ruff/black/mypy clean repo-wide throughout. **Next**: per explicit direction, the project now transitions from implementation to production hardening — no further Camera Runtime feature work is scoped until that phase defines it. |
| 2026-07-28 | **RM-12 VMS redesign: Camera Registry component implemented on `develop`, first component built against the frozen RM-12 enterprise architecture (design docs not committed to the repository — this entry is the durable record).** Closes RM-12's most-cited gap: no API path to register a camera existed before this, only the out-of-band `scripts/siv_register_camera.py`. Delivers `POST /cameras` (creates `cameras` + `camera_stream_profiles` in one transaction, encrypted RTSP credentials, admin-gated, audit-logged, publishes `CameraRegisteredEvent`), `PATCH /cameras/{id}/lifecycle` (RM-12 §10's state machine — `DRAFT → TESTING → VERIFIED → OPERATIONAL ⇄ MAINTENANCE → DISABLED`, plus an operator-retreat `TESTING → DRAFT` edge and a universal `→ DISABLED` escape hatch not literally drawn in RM-12's diagram but judged operationally necessary; idempotent same-state redelivery, publishes `CameraLifecycleChangedEvent`), and the `ai_enabled`/`recording_enabled` Desired-state flags RM-12's Runtime State Model names for Camera Registry (operator-configurable via the existing general `PATCH /cameras/{id}`, default `false` — RM-12 Design Principle 3: adding a camera must never auto-start AI, applied symmetrically to recording). New `apps/api/app/services/camera_registry.py` (`CameraRegistryService`) mirrors `IncidentService.request_transition()`'s established shape exactly (module-level transition table, dedicated exceptions). Fixed a real, pre-existing architecture violation found while in the file: `PATCH /cameras/{id}` previously let `status` (Observed state, Camera Runtime's exclusively per RM-12) be set directly — removed. `cameras.name` gained a real `UNIQUE` constraint. Two new migrations (`7a2c4e9d1b3f` lifecycle_state, `9c1f6b4a2e7d` ai_enabled/recording_enabled). Self-reviewed against RM-12 text directly (not code self-consistency) before commit — three disclosed, accepted implementation choices, not defects: (1) `status="DISCONNECTED"` is set at creation as the required initial value for a `NOT NULL` column with no Camera Runtime yet to supply a real observation — commented in the model as provisional, and Camera Runtime's first real connection overwrites it as a normal write, not a correction; (2) the two lifecycle edges named above go beyond RM-12's literal diagram; (3) `lifecycle_state` is a column on `cameras`, not the separate table RM-12's Data Architecture section named — avoids duplicating what `audit_log` already records. `docs/FRONTEND_BACKEND_CONTRACTS.md` updated (`POST /cameras`, `PATCH /cameras/{id}/lifecycle`) so the existing `test_no_undocumented_extra_routes` contract-coverage test stays meaningful. Full repo suite: 562 passed; ruff/black/mypy clean; 4 pre-existing failures unrelated to this change (verified, not fixed — three in `test_config.py` only reproduce in a `.env`-less environment like CI, one in `test_bridge.py` because this dev machine's `.venv` has genuinely working `gi` bindings unlike the environment that test assumes). Camera Runtime is next. | Claude |
| 2026-07-27 | **RM-11.SIV re-validated end-to-end against real hardware on `develop`, post-consolidation — first successful real-camera + real-model integrated run since the backend/frontend merge.** Full stack brought up and validated in sequence: backend (`uvicorn apps.api.app.main:create_app`, `/health/system` DB-healthy after applying the two outstanding Alembic migrations — a fresh Postgres instance had never been migrated, fixed as `docs/SIV_OPERATOR_RUNBOOK.md` step 0.5, see below), frontend (`bun run dev`, login → dashboard, via a local-dev-only bootstrap account since RM-12 still has no first-user provisioning mechanism — recorded as an open finding, not fixed here), camera registration (`scripts/siv_register_camera.py` against a real Hikvision RTSP camera, independently re-verified via direct DB inspection and a read-back through the exact `CameraRegistry` code path `DeepStreamRuntime` uses — ciphertext storage, transport, and resolved URL all confirmed correct), RTSP pre-flight (`check_rtsp.py`, PLAYING in 0.3s), and `scripts/run_siv.py` itself: real YOLO26M PGIE + real ViT SGIE (`configs/models.yaml` switched from placeholder to the real models already verified working in `configs/models.local.yaml` from a prior session — `enabled: true` plus the custom bbox-parser wiring; local-only change, `configs/models.yaml.bak` holds the prior placeholder state, not committed), sustained ~24.7 FPS, 5.1ms end-to-end latency, GPU 24%/2.3GB, `Model: PRODUCTION` confirmed, RM-11 Visualization's RTSP output stream live throughout. Pipeline/Camera/RTSP/PGIE/Tracker/SGIE/RuntimeAdapter/ThreatEngineRuntimeAdapter all reported healthy for the full run; ThreatEngine/Incident/Alarm/Calibration correctly stayed idle — the camera's field of view was genuinely empty the entire run (independently confirmed via a direct RTSP snapshot capture, not inferred from logs), and no calibration row exists yet for this camera (`CalibrationService.estimate()` raises `CalibrationNotFoundError`, and `threat_runtime_adapter.py` explicitly skips threat assessment when that happens) — both are expected/documented states, not defects. **Two real, independent findings surfaced and fixed along the way**, both local documentation/config bugs, not architecture: (1) `.env.example` was missing `RADAR_EYE_JWT_SECRET`, a required `EnvSettings` field with no default (fixed, committed); (2) `docs/SIV_OPERATOR_RUNBOOK.md`'s own Step 1 command (`python scripts/check_environment.py`) used plain script-path invocation instead of the module form (`python -m scripts.check_environment`) the script's own docstring specifies — the former never adds the repo root to `sys.path`, so `import apps...` fails; fixed as runbook step 1, plus the missing DB-migration prerequisite added as step 0.5. **Confirmed, not re-litigated: the RM-14 process-composition gap remains a genuine release blocker** — `apps/api` and `apps/deepstream` still construct independent `InProcessEventBus` instances when run as separate processes (this run's own `WebSocketBridge` docstring documents the same gap independently), so this validation could not exercise live DeepStream→frontend WebSocket propagation; every independently-testable component was validated regardless, per explicit instruction not to reduce validation scope because of it. **Outstanding before RM-11.SIV can be called complete**: a positive-detection test with a real person/object in frame (this run's empty-scene result cannot yet confirm YOLO26M detects a real person), camera calibration (`/calibration` in the frontend — required before any Incident can ever be produced, calibration cannot happen before a confirmed detection), then a full physical validation pass and the final `SIV_VALIDATION_REPORT.md`/`SIV_BASELINE.md`/`siv_report.json` writeup. Full `siv_report.json` for this run: `apps/siv_reports/siv_report_2026-07-27T173554.718603+0000.json`. | Claude |
| 2026-07-26 | **Repository Consolidation complete: RM-13 frontend merged into `develop` at `frontend/`.** `git subtree add --prefix=frontend` imported the standalone `radar-eye-command` repository's full 9-commit history (Phase 0 through the post-RM-13 deliverables) onto a freshly-recreated `feature/frontend-integration` branch (the prior placeholder at this name carried zero real work and was not reused as a base — deleted and recreated from current `develop` instead), verified there (`bunx tsc --noEmit` clean, `eslint` 0 errors, dev server smoke-tested, content byte-identical to the source repo except gitignored `node_modules`/`.tanstack`), then merged into `develop`. Radar-Eye is now the single source repository for backend and frontend — no submodule, no permanently separate frontend repository, no path rewrites to any existing backend directory. `docs/FRONTEND_CONTEXT.md` and `docs/FRONTEND_GAP_ANALYSIS.md` updated to reflect the integrated state (both previously read as if RM-13 hadn't started). Repository Consolidation is now fully complete (backend: RM-11 Visualization + RM-12; frontend: RM-13) — see the Repository Integration Completion Report for full detail. RM-14 not yet started. | Claude |
| 2026-07-26 | **Repository Consolidation (Step 1 of 2, backend) complete: RM-12 API Service merged into `develop`.** `feature/RM-12-api-service` → `feature/api` (`3180ea7`) → `develop` (this merge), per the Repository Integration Audit's approved merge order (RM-11 Visualization first, RM-12 second — see the entry immediately below). One conflict, in this file only (Next Immediate Action, Milestone Status, Subsystem Status, Recently Completed — the same recurring pattern as the RM-11.SIV merge on 2026-07-23), resolved by combining both branches' entries, not discarding either. Post-merge quality gates re-verified clean: ruff clean, black clean, mypy clean, 398 passed / 0 failed (skip count varies by sandbox Postgres role, documented throughout this file). `develop` now has a complete DeepStream pipeline and a working API layer in front of it for the first time. Next: RM-13 frontend `git subtree` migration (Step 2 of 2). | Claude |
| 2026-07-25 | **RM-12 (API Service) implemented on `feature/RM-12-api-service` — all 6 phases complete, not yet reviewed or merged.** Phase 1: JWT auth (`PyJWT`, HS256, access/refresh pair), bcrypt password hashing, `LocalUserAuthProvider` (ABC-based, LDAP-swappable per ADR-009), `get_current_user`/`require_role` dependencies, `POST /auth/login`/`/auth/refresh`, the additive `ApiError`/`ApiResponse.error` envelope wired through global exception handlers. Phase 2: `audit_log` table/model/repository/Alembic migration (`docs/DATABASE_SCHEMA.md` was missing this table despite ADR-008 already mandating it -- added as a documentation correction, approved before Phase 2 began) and `AuditLogger` (participates in the caller's own transaction, so a mutation and its audit record can never diverge). Phase 3: 7 read-only routers (cameras, threats, incidents, reviews, calibration, evidence, analytics) covering every `GET` route in `docs/FRONTEND_BACKEND_CONTRACTS.md`; resolved two open design points the plan flagged for implementation time — `GET /threats/active` reads from a new in-memory `ActiveThreatCache` (honestly empty until Phase 5's event-bus feed exists), and evidence `/download` routes stream files directly (no signed-URL infrastructure, matching the single-node air-gapped deployment). Phase 4: mutating routers for incidents (`PATCH`, operator, routed only through `IncidentService.request_transition()` — see the separate `[Cross-Subsystem]` entry), reviews (`PATCH` + 4 semantic `POST` actions matching CLAUDE.md's Human Review Rules exactly), cameras (`PATCH`, admin), calibration (`POST start`/`validate`, thin wrappers over `CalibrationService`'s existing `calibrate()`/`estimate()`), and users (`GET`/`PATCH /users/{user_id}`, admin, role-only) — every mutating route audit-logged. `GET`/`PATCH /config` explicitly descoped (`docs/OPEN_QUESTIONS.md` Q-014 — no configuration persistence model exists in the architecture docs). Two real bugs found via live verification against a real PostgreSQL instance, not just the automated suite: building response schemas *after* `session.commit()` (which expires ORM attributes, causing a `MissingGreenlet` on the next attribute read) and reading an `onupdate=func.now()` column right after `flush()` without an explicit `session.refresh()` (an UPDATE's server-computed value, unlike an INSERT's `server_default`, isn't auto-repopulated). Phase 5: `ConnectionManager` + `WebSocketBridge` (one `EventBus.subscribe()` per event type at app startup, not per connection) wiring 5 of 6 `/ws/*` channels (`threats`, `incidents`, `camera_health`, `reviews`, `alarms`); `/ws/tracking` explicitly descoped (Q-015 — no event model/payload/publisher exists anywhere for live tracking data). New WS test harness (`starlette.testclient.TestClient`, since `httpx` has no WebSocket support) including the explicit end-to-end latency acceptance test (live-measured ~0.5ms publish-to-receive, well under the 2s bound). Documented a real process-topology limitation: `apps/api` and `apps/deepstream` each construct independent `InProcessEventBus` instances when run separately today; wiring them to share one bus in production is RM-14's job (process composition), not yet decided, matching `apps/deepstream/app/main.py`'s own existing precedent for this exact question. Phase 6: a new consolidated contract-coverage test (`apps/api/tests/test_contract_coverage.py`) asserting every implemented route/channel exists and every descoped one is (still) absent, against the live OpenAPI schema and route table, not just recalled from memory. Two genuine architecture/documentation gaps found and corrected along the way, each reviewed and approved before proceeding: `docs/DATABASE_SCHEMA.md` was missing the `audit_log` table ADR-008 already mandated; `docs/FRONTEND_BACKEND_CONTRACTS.md`'s `PATCH /users` (no identifier) was a documentation error, corrected to `PATCH /users/{user_id}`. Full repo suite: **398 passed, 116 skipped, 0 failed**; ruff/black/mypy clean; 83% coverage repo-wide (new RM-12 code itself at or near 100% outside DB-dependent branches that skip in this sandbox for the same pre-existing `test_user`/`radar_eye` Postgres-role mismatch documented throughout this file). `feature/RM-12-api-service` retained pending Principal Engineer review before merging into `feature/api` → `develop`. |
| 2026-07-25 | **[Cross-Subsystem] IncidentService — `IncidentService.request_transition()` added on `feature/RM-12-api-service`.** RM-12 Phase 4 (`PATCH /incidents/{id}`) needs a public, validated entry point for non-system-pipeline-driven status changes (docs/RM-12_ARCHITECTURE.md §3.3, found missing during the Architecture Readiness Review — `IncidentService`'s only transition path, `_transition()`, was private and called only from `handle_escalation()`/`sweep_track_lost()`). `services/incident_service/` is owned by `feature/incident-service` (currently idle — RM-07/RM-10 done, merged to `develop`), not `feature/api`; this is an intentional, explicitly-approved cross-subsystem touch (Principal Engineer decision: implement directly on the RM-12 branch as its own standalone, clearly-labeled commit, rather than a separate ticket branch off `feature/incident-service`), not an ownership transfer — `feature/incident-service` remains the owning branch for `services/incident_service/`, and `IncidentService` remains the sole owner of every incident lifecycle transition. `request_transition(incident, new_status, *, now)` validates the request against a new `EXTERNALLY_REQUESTABLE_TRANSITIONS` map (docs/INCIDENT_LIFECYCLE.md's Ownership section: System owns NEW/ACTIVE, Operator owns ACKNOWLEDGED/RESOLVED, System Retention Service owns ARCHIVED) and then calls the *same* private `_transition()` that `handle_escalation()`/`sweep_track_lost()` already use — one canonical state machine, no duplicate/divergent transition logic, no repository writes from any router. Named generically (not `operator_transition`) so any future non-pipeline caller — API, automation, CLI — can validate through the same path. 7 new unit tests (`tests/services/incident_service/test_service.py::TestRequestTransition`); also live-verified against a real PostgreSQL instance (ACTIVE→ACKNOWLEDGED→RESOLVED, rejected ACKNOWLEDGED→ACTIVE and RESOLVED→ARCHIVED, then rolled back — no data left committed). Full repo suite: 384 passed, 90 skipped, 0 failed; ruff/black/mypy clean. RM-12 Phase 4 continues on `feature/RM-12-api-service`, now consuming this method from `apps/api/app/routers/incidents.py`. |
| 2026-07-24 | **RM-11 CLOSED — RM-11.SIV Visualization addendum merged into `feature/deepstream`.** PR #8 (`feature/RM-11-SIV-visualization` → `feature/deepstream`) merged via a regular merge commit (`387a1ee`, never squashed) after a code review round: one real implementation defect found and fixed (`VisualizationManager.mark_failed()` wasn't tearing down a partially-built/failed-to-start visualization branch, leaving it running and consuming GPU/encoder resources indefinitely even though `health()` correctly reported it as failed — fixed by reusing the existing `teardown()` call, 3 new unit tests via a test double, re-verified on real hardware reproducing the exact RTSP-port-conflict scenario), plus a cosmetic dual-logging cleanup and a forward-looking multi-camera UDP-port note recorded in `docs/PERFORMANCE_OPTIMIZATION_PROGRAM.md` (not implemented). Post-merge verification: clean checkout, full quality gates green (398 passed, 0 failed, 55 skipped, ruff/black/mypy clean), baseline tag `rm-11-development-siv-baseline` confirmed as an ancestor of the merge commit, `docs/RM-11_SIV_ENGINEERING_REVIEW.md` and `artifacts/RM-11.SIV-FINAL-VALIDATION/` confirmed byte-identical to the tag (frozen evidence untouched by the merge). `feature/deepstream` is 16 commits ahead of `develop` — promotion not yet requested/performed. RM-12 (API Service) is now the current roadmap milestone; planning phase (`docs/RM-12_ARCHITECTURE.md`/`docs/RM-12_IMPLEMENTATION_PLAN.md`) begins next, no implementation before both are reviewed and approved. |
| 2026-07-23 | **RM-11.SIV Visualization subsystem implemented and hardware-verified on `feature/RM-11-SIV-visualization`** (cut from `feature/deepstream`, not yet merged). Adds an operator-visible RTSP video stream (bounding boxes, track IDs, SGIE classification, threat/zone/distance, FPS/latency) rendered from the existing pipeline's own inference results via an optional `tee` after SGIE — no second inference pass, byte-for-byte identical pipeline when disabled. See Design note (RM-11, SIV Visualization) for full architecture, the real-hardware video-corruption defect found and fixed (`iframeinterval`), and the OFF-vs-ON performance comparison (`docs/SIV_BASELINE.md`). 9 commits, each its own quality-gated phase; full repo suite green (395 passed, 55 skipped, ruff/black/mypy clean). |
| 2026-07-23 | **CI fully green + `check_environment.py` operator pre-flight tool merged into `develop`**, via `feature/RM-11-SIV-runbook` → `feature/deepstream` → `develop` (regular merge commits). Fixed all 21 mypy findings and both reported runtime test failures — the FIRE fast-path double-beating the `incident` heartbeat once per `EscalationSignal` instead of once per incident actually engaged that frame (two signals, `INCIDENT_ELIGIBLE` and `ALARM_ELIGIBLE`, both idempotently resolving to the same incident), and `Watchdog.check_once()`'s falling-edge test racing real elapsed time against a `0.0` threshold (fixed by giving it the same injectable `now` `HeartbeatRegistry.status()` already supports). The reported "database setup" log noise (test_user db, constraint violations, schema-creation ordering) was investigated against every `db_engine`/`db_session` fixture and `configs/settings.yaml` — all correctly target the CI Postgres service's actual database; no code defect found. Added `scripts/check_environment.py` — validates GPU/driver/CUDA/TensorRT/DeepStream/GStreamer/PostgreSQL/Python version/required packages/env vars/model+engine+label files/disk space/writable output directories in one command, PASS/FAIL per check with the exact reason — now step 1 of `docs/SIV_OPERATOR_RUNBOOK.md`, with a new operator decision tree at the top showing the full check-gated command sequence. Post-merge quality gates re-verified clean on all three branches: 358 passed, 0 failed, 55 skipped, ruff/black/mypy clean. |
| 2026-07-23 | **RM-11.SIV merged into `develop`** via a regular merge commit (`feature/RM-11-SIV` → `feature/deepstream` → `develop`), following the Principal Engineer's explicit merge instruction. `docs/IMPLEMENTATION_STATUS.md` conflicted (both branches had independently updated Next Immediate Action/Milestone Status/Subsystem Status/Recently Completed/Changelog since diverging) — resolved by combining both branches' entries, not discarding either. Post-merge quality gates re-verified on `develop`: **348 passed, 0 failed, 53 skipped** (DB-dependent tests skip without reachable PostgreSQL, not failures), ruff/black clean repo-wide. `feature/deepstream` retained as the long-lived owning branch, `feature/RM-11-SIV` retained (not deleted). RM-11 Phase 3 (multi-camera scaling) requires its own design review before implementation begins. |
| 2026-07-23 | **DeepStream-Yolo real-model integration path reviewed and wired in, on `feature/RM-11-SIV`.** At the Principal Engineer's request, reviewed `~/Downloads/DeepStream-Yolo/` (a pre-existing, out-of-repo working build) for what RM-11.SIV's config surface was missing to load it. Vendored the required custom `nvinfer` bbox-parser *source only* (`apps/deepstream/native/nvdsinfer_custom_impl_Yolo/`, MIT-licensed, `scripts/build_yolo_parser.sh` to build it -- never its compiled output or any model/engine binary); extended `ModelStageSettings`/both templates/`ModelConfigResolver` with the properties a real working config needs (`custom_lib_path`, `parse_bbox_func_name`, `cluster_mode`, `network_type`, `maintain_aspect_ratio`, `symmetric_padding`, `topk`, `operate_on_class_ids`, `input_object_min_width`/`height`, `infer_dims`), all optional so the placeholder path is unaffected. Found and fixed a real defect in the process: `infer-dims` was previously hardcoded to a guessed value on both stages, when the correct behavior (confirmed against the real working config) is to omit it and let nvinfer auto-detect from the ONNX graph. Verified by building the vendored `.so` locally (correct symbols export) and confirming `ModelConfigResolver`'s output, pointed at the real model files, matches the Principal Engineer's independently hand-authored working configs. `docs/MODEL_REGISTRY.md` updated with confirmed TensorRT/DeepStream compatibility and known classes for both models -- approval status unchanged, still gated on benchmarking. 4 new commits; full test suite green (13 new/updated unit tests for the resolver extension); ruff/black clean. |
| 2026-07-23 | **RM-11.SIV implemented and hardware-verified on `feature/RM-11-SIV`.** All 10 tasks from the Principal Engineer approval: external model configuration (`configs/models.yaml`/`ModelConfigResolver`, superseding the Phase 1/2 placeholder-config mechanism entirely, falling back to it when a stage is disabled); camera bootstrap (`scripts/siv_register_camera.py`, env-var-substituted `configs/camera.yaml`, never read by the runtime directly -- `CameraRegistry` still owns DB-backed loading); 17 named stage loggers plus an operator-facing audit logger; a `HeartbeatRegistry` shared by a validation watchdog (visibility only) and a console dashboard (Unified Heartbeat's single source of truth); a full frame-execution trace (off by default, near-zero overhead when disabled); PGIE/SGIE fps and event/threat/alarm/incident throughput; an automatic `siv_report.json` per run; `scripts/run_siv.py` as a separate composition root (production `main.py` untouched). See Design note (RM-11, SIV) for full detail. **Found and fixed two real defects via this milestone's own real-hardware verification** -- a pre-existing `nvstreammux` `batched-push-timeout` gap (since RM-11 Phase 0) that silently blocked all frame flow whenever active sources are fewer than `batch-size` (true for every camera count below 20), and a `pipeline_fps`/`camera` Unified-Heartbeat consistency bug introduced within this same milestone. Both fixed and re-verified on hardware; results recorded in `docs/SIV_VALIDATION_REPORT.md`. 15 commits on `feature/RM-11-SIV`; full unit test suite green (SDK-free tests for every new component; DB-backed tests for the bootstrap script and the full calibration-through-alarm heartbeat chain skip without reachable PostgreSQL, consistent with every other DB-dependent test in this repo); ruff/black clean. |
| 2026-07-22 | **RM-08 test fix merged into `develop`** — resolves `tests/services/recording/test_service.py::test_disk_usage_warning_threshold`, the last remaining repository test failure. Root cause: the test asserted on `InProcessEventBus`-delivered events immediately after `RecordingService.check_storage_and_purge()` returned, but `publish()` only enqueues onto the subscriber's own background task (RM-04 design review — best-effort, asynchronous delivery) and does not wait for it to run; this raced that task and failed deterministically under this environment's scheduling. `RecordingService`'s actual behavior (emitting a CRITICAL `SystemEvent` above the configured disk threshold) was always correct. Fixed by switching to the `asyncio.Queue` + `wait_for` pattern already established elsewhere in this repo — test-only change, no production code touched. Verified deterministic across 5 repeated runs. **Repository quality gates are now fully green**: 278 passed, 0 failed, 49 skipped, ruff/black clean — the first such result recorded in this file. `feature/recording` retained as the owning branch. |
| 2026-07-22 | **RM-11 Phase 2 merged into `develop`** via a regular merge commit, following Principal Engineer review — **APPROVED WITH ONE REQUIRED FOLLOW-UP**, completed before merge. Review confirmed: scope complete and correctly bounded; `ThreatEngineRuntimeAdapter` correctly separated from Runtime Adapter/Threat Engine/Calibration/Incident/Alarm with business logic ownership intact; SGIE placeholder-only with no semantic label interpretation; Calibration accessed only through `estimate()`; Threat Engine has no duplicate decision logic; Incident/Alarm sessions correctly escalation-scoped, not frame-scoped; hardware verification demonstrated every component and correctly distinguished mechanical execution from operational accuracy. Required follow-up (latency instrumentation) investigated and resolved per the review's required steps rather than assumed — see the separate Recently Completed entry for the diagnostic and fix. Post-merge quality gates re-verified: 277 passed / 49 skipped (same single pre-existing, unrelated Recording Service failure), ruff/black clean repo-wide. `feature/deepstream` retained as the long-lived owning branch. RM-11 Phase 3 (multi-camera scaling) requires its own design review before implementation begins. |
| 2026-07-22 | **RM-11 Phase 2 latency-instrumentation follow-up completed**, per the Principal Engineer review's required investigation. Real-hardware diagnostic (`hash`/`id(gst_buffer)` vs. buffer PTS at the ingress/egress probes): 15/15 matches — the buffer-identity correlation was always reliable; no change needed there. Root cause: `PerformanceInstrumentation` reported only the single most-recent frame's latency, highly sensitive to sampling timing (true per-frame latency ranges ~120ms during pipeline startup down to a stable ~2ms at steady state). Fixed by windowing latency like FPS already was — `end_to_end_latency_ms` is now a rolling average, no application/orchestration behavior changed. Re-verified on hardware: 1.87ms, stable and consistent with the diagnostic; every other Phase 2 result unchanged between runs. 3 new unit tests. Ready for merge into `develop` per the review's pre-authorization. |
| 2026-07-22 | **RM-11 Phase 2 implemented and hardware-verified**, per the approved implementation order (SGIE → ThreatEngineRuntimeAdapter → Calibration → Threat Engine → Incident → Alarm → Event publication → hardware verification). See Design note (RM-11, Phase 2) for full detail. Delivers `apps/deepstream/configs/sgie_placeholder.txt` (placeholder secondary classifier) and `apps/deepstream/app/threat_runtime_adapter.py` (`ThreatEngineRuntimeAdapter` — the orchestration layer RM-07/09/10 already anticipated), wiring `CalibrationService` → `ThreatEngine` → `IncidentService`/`AlarmService` → `EventBus`. Hardware verification: zero errors, SGIE genuinely classified real detections, 5582 `ThreatAssessmentEvent`s published, 64 `AlarmService` activations, tracking behavior unchanged from Phase 1. Calibration/Incident were mocked at the class level for the hardware run (no reachable PostgreSQL in this sandbox) — separately covered by 19 new DB-backed unit tests plus an isolated mocked-DB smoke test that confirmed exact orchestration call sequencing. Discovered (not fixed) a real instrumentation concern: `end_to_end_latency_ms` reads suspiciously low with SGIE added, suggesting the buffer-identity-based ingress correlation from Phase 1 may not survive a third pipeline stage — both phases' latency figures are now flagged unvalidated pending investigation; `inference_fps` is unaffected and remains trustworthy. All quality gates green except the one pre-existing unrelated Recording Service failure. Not yet reviewed or merged into `develop` — RM-11 Phase 3/further optimization may not proceed until this review is approved. |
| 2026-07-22 | `CalibrationService` in-memory homography cache merged into `develop` (`feature/calibration`) — RM-11 Phase 2 prerequisite per that design review's Decision A. See Design note (RM-05) for detail. 6 new/updated tests (DB-dependent ones skip without reachable PostgreSQL, unchanged pattern); cache branch logic additionally smoke-tested with a stubbed repository. Quality gates re-verified post-merge: 271 passed / 46 skipped (same one pre-existing unrelated failure), ruff/black clean. |
| 2026-07-22 | **RM-11 Phase 1 merged into `develop`** via a regular merge commit, following Principal Engineer review — **APPROVED FOR MERGE INTO DEVELOP**, no blocking findings. Review confirmed: scope matches the approval exactly (PGIE, NvDCF, single-camera, performance instrumentation, nothing out-of-scope); PGIE's placeholder detector is correctly isolated with no production assumptions embedded; NvDCF correctly positioned after PGIE with persistent track IDs demonstrated on physical hardware; ADR-027 correctly implemented (Runtime Adapter is the sole owner of `pyds`/DeepStream metadata/SDK-specific translation, no architectural leakage); observation construction correctly separated from extraction for testability; the monotonic/wall-clock timestamp correction is architecturally correct and "should remain permanent"; instrumentation stays lightweight via periodic sampling; hardware validation retires the remaining Phase 1 technical uncertainty; no new regressions (pre-existing Recording Service failure confirmed outside RM-11 ownership). Post-merge quality gates re-verified: 271 passed / 43 skipped (same single pre-existing failure), ruff/black clean repo-wide. `feature/deepstream` retained as the long-lived owning branch. Authorization granted for an RM-11 Phase 2 design review to be presented — implementation must not begin until that review is itself approved. |
| 2026-07-22 | **RM-11 Phase 1 implemented and hardware-verified**, per the approved scope (PGIE integration, NvDCF integration, single-camera execution, performance instrumentation; SGIE/Threat Engine/Calibration/Incident/Alarm explicitly out of scope). See Design note (RM-11, Phase 1) for full detail: `nvinfer`/`nvtracker` added to the pipeline (placeholder model, Decision C); `apps/deepstream/app/observations.py` (repository-native domain objects) and `instrumentation.py` (all 8 required metrics) added; `RuntimeAdapter` extended as the sole `pyds`-importing module (ADR-027). A real bug (monotonic-vs-wall-clock timestamp conflation) was caught and fixed during implementation. Hardware verification (30s single-camera run, real RTSP ingestion, real inference): zero errors, PGIE ran real inference (750 frames, 5618 detections), NvDCF assigned persistent track IDs (117/130 tracks lasted ≥3 frames), `RuntimeAdapter.last_observation` confirmed as a genuine `FrameObservation`, 25.4 fps / ~25ms latency at steady state. `pipeline_startup_seconds` (~585s) is a placeholder-model/environment artifact (uncacheable TensorRT engine rebuild plus GPU contention from an unrelated process on this shared dev machine), not a code defect or representative of the real model. 31 new unit tests passing; ruff/black clean; full suite green except the one pre-existing unrelated Recording Service failure. Not yet reviewed or merged into `develop` — a Phase 1 design review is required before Phase 2 begins, per the approval's acceptance criteria. |
| 2026-07-22 | **RM-11 architectural foundation merged into `develop`**, independently of Phase 1 implementation, via a regular merge commit — reference environment verification, Runtime Verification Review approval, and ADR-027 (DeepStream Runtime Adapter as Anti-Corruption Layer) all now on `develop`. Per the approval: "Once ADR-027 is on develop, I would consider the architectural foundation for RM-11 complete. At that point, Phase 1 can proceed against a stable, repository-wide contract rather than a milestone-local design note." Post-merge quality gates re-verified: 249 passed / 43 skipped (same single pre-existing, unrelated Recording Service failure as every prior check), ruff/black clean repo-wide. `feature/deepstream` retained as the long-lived owning branch. |
| 2026-07-22 | **ADR-027 accepted**: DeepStream Runtime Adapter as Anti-Corruption Layer (`ADR_INDEX.md`). Elevates the Runtime Verification Review's anti-corruption-layer constraint from an RM-11-scoped design note to a formal, cross-milestone architecture decision — explicitly "applies to all present and future milestones and subsystems, not RM-11 alone." Prohibits `pyds`, `NvDsBatchMeta`, `NvDsFrameMeta`, `NvDsObjectMeta`, `NvDsClassifierMeta`, `NvDsUserMeta`, `Gst.Buffer`, `Gst.Sample`, and any other DeepStream/GStreamer-specific runtime type from crossing the Runtime Adapter boundary; permits only repository-native domain objects beyond it (e.g. `FrameObservation`, `DetectionObservation`, `TrackObservation`, `DistanceEstimate`, and the existing `shared/events` types). Restricts `pyds`/`gi.repository.Gst`/`gi.repository.GLib` imports to `apps/deepstream/` only. Rationale: if the inference backend is ever replaced (TensorRT, Triton, ONNX Runtime, OpenVINO, CPU inference, simulation, recorded playback), only the Runtime Adapter should require modification. No code changes required — current Phase 0 code already complies (`runtime_adapter.py` has no `pyds`/`Gst`/`GLib` imports at all yet). |
| 2026-07-22 | RM-11 Runtime Verification Review: **APPROVED**. Confirms on physical hardware that DeepStream runtime, TensorRT inference, GStreamer integration, `NvDsBatchMeta` extraction, Runtime Adapter integration, `AsyncBridge` execution, and `EventBus` delivery all work end-to-end (see the prior environment-verification entry) — retires RM-11's principal pre-identified technical risk. Establishes a new, effective-immediately architectural constraint: the Runtime Adapter is the anti-corruption layer between the NVIDIA SDK and the domain model — `pyds`/`NvDsBatchMeta`/`NvDsFrameMeta`/`NvDsObjectMeta` must never propagate past it; every downstream service receives only repository-native domain objects. Recorded as a new Design note (RM-11, Runtime Verification) row. No code changes required — nothing in current Phase 0 code touches `pyds` types, and the environment-verification smoke test already followed this shape (parsed metadata stayed in a standalone probe callback, only a plain string summary reached the Runtime Adapter). Governs Phase 1/2 implementation going forward. |
| 2026-07-22 | RM-11 (DeepStream AI Pipeline) **Phase 0** merged into `develop` via a regular merge commit, following Principal Engineer review — **APPROVED FOR MERGE INTO DEVELOP**, no blocking findings. Review confirmed: Phase 0 scope is infrastructure-only (no PGIE/SGIE, no model-dependent logic); the GLib↔asyncio bridge is the single concurrency boundary and no subsystem/service crosses it directly; the Runtime Adapter is limited to camera lifecycle/disconnect/event publication with Threat Engine/Incident/Alarm/Calibration integration correctly deferred; ingestion, health integration, and pipeline builder are each correctly scoped and isolated. Outstanding issues (pre-existing Recording Service test failure, numpy-stub/Python 3.13 mypy issue, absent DeepStream runtime libraries) confirmed external to RM-11, non-blocking. Post-merge quality gates re-verified on `develop`: 249 tests passing / 43 skipped (same one pre-existing, unrelated Recording Service failure as before the merge), ruff/black clean repo-wide. `feature/deepstream` retained as the long-lived owning branch. Approved next phase: RM-11 Phase 1, scoped strictly to PGIE integration, NvDCF integration, single-camera execution, placeholder model configuration, and performance instrumentation — SGIE, Threat Engine, Calibration, Incident, and Alarm integration remain out of scope until Phase 1 is itself reviewed and approved. |
| 2026-07-21 | RM-11 (DeepStream AI Pipeline) **Phase 0** implemented on `feature/deepstream` (synced with `develop` first — branch was 72 commits stale since bootstrap) following an approved design review (five decisions: single-process runtime topology via `AsyncBridge`; calibration caching deferred to `CalibrationService` itself; model-readiness proceeds via placeholder PGIE/SGIE config; 20-camera authoritative target; `BENCHMARK_ACCEPTANCE_CRITERIA.md` governs calibration acceptance until `CAMERA_CALIBRATION_SPEC.md` defines its own). Delivers `apps/deepstream/app/`: `config.py` (settings), `bridge.py` (`AsyncBridge` — GLib main-loop thread ↔ asyncio bridge, the design review's sole concurrency boundary), `ingestion/reconnect.py` (per-camera exponential backoff, INV-012 isolation), `ingestion/camera_registry.py` (loads `Camera`+`CameraStreamProfile` via `apps.api`'s repositories, decrypts RTSP URLs), `ingestion/source.py` (RTSP source bin construction — rtspsrc→rtph264depay→h264parse→nvv4l2decoder; requires real GStreamer/DeepStream, untestable on this dev machine), `health/heartbeat.py` (`FrameCounter` + `HeartbeatScheduler` reporting into RM-09's `HealthCollector`), `pipeline/builder.py` (`nvstreammux` + source bin management, Phase 0 fakesink tail — no PGIE/SGIE yet), `runtime_adapter.py` (Phase 0 scope: camera connect/disconnect → `CameraDisconnectedEvent`/`SystemEvent`, the component RM-07/RM-09/RM-10's design notes already named "the Threat Engine Runtime Adapter, deferred until RM-11"), `runtime.py` (composition/lifecycle, reconnect orchestration), `main.py` (Phase 0 dev/standalone entrypoint — not RM-14's final packaging decision). Added a `deepstream:` section to `configs/settings.yaml`. 28 new unit tests (`apps/deepstream/tests/`) covering everything not requiring real GStreamer bindings (reconnect policy, bridge threading/scheduling via an injected fake main loop, heartbeat, Runtime Adapter event publication against the real `InProcessEventBus`); 3 DB-backed camera-registry tests skip without reachable PostgreSQL, matching repo convention. ruff/black clean; mypy blocked repo-wide by a pre-existing numpy-stub/Python-3.13 environment incompatibility unrelated to this change (reproduces on unmodified `services/calibration` too). Not yet pushed, reviewed, or merged — local commits only (no push credentials in this environment). Phase 1 (PGIE/NvDCF) not started; blocked on real Jetson/DeepStream hardware to validate `ingestion/source.py`/`pipeline/builder.py`, and on sourcing/benchmarking the model files per `MODEL_REGISTRY.md`. |
| 2026-07-21 | **Repository Integration Audit corrections applied** (Category B, architecturally reviewed and approved individually): `shared/schemas/health.py` ownership reassigned to `feature/shared-contracts` (`8a063ae`); RM-09 health routes corrected to match `FRONTEND_BACKEND_CONTRACTS.md` exactly, dropping the undocumented `/api/v1` prefix and adding the previously-missing `GET /health/recording` (`cb43176`); RM-10's `AlarmService.on_alarm_requested()` (bus subscription to an event nothing produces) replaced with `AlarmService.trigger()` (direct in-process call mirroring `IncidentService.handle_escalation()`) (`e64803a`). All three merged into `develop` (`aebc650`). 262/262 tests passing, ruff/black/pre-commit clean, mypy at the unchanged 9-finding advisory baseline. |
| 2026-07-21 | **Repository Integration Audit (Category A) CI fix applied**: `tests/services/recording/test_service.py`'s shared fixture created `Camera(status="ACTIVE")`, an invalid `CameraConnectionStatus` value violating the `ck_cameras_status` CHECK constraint — failed 4 tests deterministically against real PostgreSQL (`test_create_snapshot`, `test_create_event_clip`, `test_on_incident_created_handler`, `test_repository_helpers`). This was CI's actual failure, reproduced and confirmed by full stack trace, not guessed. Corrected to `"CONNECTED"` on `feature/recording` and merged into `develop` (`eb80814`). CI restored to green. |
| 2026-07-21 | **Repository Integration Audit conducted and reviewed**: a second engineer (`laurent-rodz`) implemented and merged RM-08, RM-09, and RM-10 directly into `develop` without a presented design review or a recorded Principal Engineer review, breaking from every prior milestone's process. A full audit (repository state, architecture compliance, subsystem ownership, public interfaces, CI-failure root cause reproduced against a real PostgreSQL instance matching CI exactly, quality review, documentation accuracy) found: (1) a genuine CI-breaking test defect in RM-08 (see the Category A entry below); (2) an unreviewed `shared/` ownership violation and a REST-path mismatch against `FRONTEND_BACKEND_CONTRACTS.md` in RM-09; (3) an unreviewed, unreconciled integration-pattern choice in RM-10 (`AlarmRequestedEvent` bus subscription with no producer); (4) RM-08's own docs commit had deleted the RM-03/RM-04/RM-06/RM-07/RM-05 Design Note rows and the Primary branches/Branching model rows from this file's Snapshot table (restored above); (5) this file's Recently Completed entries below for RM-08 and RM-09 claim "following Principal Engineer review approval" and "All quality gates clean" — **neither claim is substantiated by any review record, and the quality-gates claim was proven false by reproduction.** Those entries are left as-written (not edited) as an honest record of what was claimed at the time; this entry and the two Category A/B entries above are the correction. The audit report itself was reviewed and approved, with findings classified into Category A (mandatory, fixed immediately), Category B (architecture review required — three items, each individually reviewed and approved before any implementation), and Category C (this documentation correction, performed only after A and B were resolved). |
| 2026-07-21 | RM-10 (Alarm Service) implemented on `feature/incident-service`. Delivers `services/incident_service/alarm.py` (`AlarmService`, `AlarmAdapter`, `MockAlarmAdapter`, `AlarmRecord`, `AlarmState`, `AlarmTargetType`), re-exported in `services/incident_service/__init__.py`. Implements hardware-agnostic alarm integration (ADR-012), strict HIGH/FIRE-only threat eligibility filtering (ADR-026), manual silence/override controls, fail-safe shutdown behavior (`stop()`), and `SystemEvent` audit log publication onto the event bus. 14 new unit tests (`tests/services/incident_service/test_alarm.py`). All quality gates clean (261 tests total repo-wide, 100% coverage on `alarm.py`). |
| 2026-07-21 | RM-09 (Health & Monitoring) merged into `develop` via a regular merge commit, following Principal Engineer review approval. Delivers `shared/schemas/health.py` (`GPUHealthSchema`, `StorageHealthSchema`, `CameraHealthSummarySchema`, `SystemHealthSchema`), `apps/api/app/health/collector.py` (`HealthCollector` GPU/storage metrics, camera heartbeat/FPS tracking, stalled stream detection), `apps/api/app/routers/health.py` (REST endpoints `/api/v1/health/system`, `/api/v1/health/gpu`, `/api/v1/health/storage`, `/api/v1/health/cameras`, `/api/v1/cameras/{id}/health`), and 6 new unit & API integration tests (`apps/api/tests/test_health.py`). All quality gates clean (248 tests total repo-wide). `feature/api` retained as a long-lived subsystem branch. |
| 2026-07-21 | RM-08 (Recording & Evidence Service) merged into `develop` (commit `f5ad06c`) via a regular merge commit, following Principal Engineer review approval. Delivers `services/recording/types.py` (`RecordingConfig`, `SnapshotResult`, `ClipResult`), `services/recording/storage.py` (`StorageManager`, filesystem storage layout generator, disk quota monitor, continuous retention sweep), `services/recording/service.py` (`RecordingService` snapshot capture, -10s/+20s event clip extraction, event bus integration, storage warning alerts). Extended `SnapshotRepository` and `RecordingRepository` in `apps/api/app/repositories/recording.py`. 6 new unit/integration tests (`tests/services/recording/test_service.py`). All quality gates verified clean post-merge. `feature/recording` retained as a long-lived subsystem branch. |
| 2026-07-20 | RM-05 (Calibration Service) merged into `develop` (commit `29e0261`) via a regular merge commit, following Principal Engineer review — approved with no blocking issues. `feature/calibration` was synced with `develop` first (43 commits behind, stale since project bootstrap). Delivers `services/calibration/service.py`: `CalibrationService` -- `calibrate()` (computes and persists a ground-plane homography from >=4 reference points, ADR-016), `estimate()` (projects an image point to distance + `DistanceZone` using a camera's latest calibration, raises `CalibrationNotFoundError` if none exists). `services/calibration/homography.py` implements DLT homography estimation/projection via numpy (no OpenCV dependency). Extends `CameraCalibrationRepository` with `get_latest_for_camera()`/`list_for_camera()`. 24 new tests (synthetic-geometry accuracy against `BENCHMARK_ACCEPTANCE_CRITERIA.md`'s tolerances of <=2m@20m/<=5m@50m, append-only history, missing-calibration error path, `CalibrationUpdatedEvent` publication against the real bus). Also fixed a pytest module-collision issue (`--import-mode=importlib` in `pytest.ini`) that would have recurred as more services add their own `test_service.py`. All gates re-verified clean on `develop` post-merge: 236 tests passing, 100% coverage. `feature/calibration` retained as a long-lived subsystem branch. |
| 2026-07-20 | RM-07 (Incident Service) merged into `develop` (commit `90c21cc`) via a regular merge commit, following Principal Engineer review — approved with no blocking issues. Delivers `services/incident_service/service.py`: `IncidentService` — `handle_escalation()` (idempotent create-or-return, no timing logic), `on_threat_assessment()` (metadata + track-lost bookkeeping from the real bus stream), `sweep_track_lost()` (10s auto-close, ADR-025). Extends `IncidentRepository` with `get_active_for_track()`. 10 new tests (creation + event publication, metadata caching, idempotency, the concurrent create-race fallback verified against a real `IntegrityError`, and the full track-lost sweep behavior). Also fixed a mypy module-resolution issue (`explicit_package_bases`/`namespace_packages` in `pyproject.toml`) that would have recurred as more services add their own test `conftest.py`. All gates re-verified clean on `develop` post-merge: 221 tests passing, 100% coverage. `feature/incident-service` retained as a long-lived subsystem branch. |
| 2026-07-20 | RM-04 (Internal Event Bus) merged into `develop` (commit `832cc6e`) via a regular merge commit, following Principal Engineer review — approved with no blocking issues. Delivers `shared/events/bus.py`: `EventBus` abstract contract, `InProcessEventBus` initial implementation (per-subscriber bounded queues, configurable publish timeout with drop-and-CRITICAL-alert on back-pressure, per-producer-per-event-type ordering, full subscriber fault isolation). 9 new tests. Also corrected stale "event bus" wording in `PROJECT_CONTEXT.md` that had listed it under `apps/api` from before RM-04's branch ownership was settled. All gates re-verified clean on `develop` post-merge: 211 tests passing, 100% coverage. `feature/shared-contracts` retained as a long-lived subsystem branch. |
| 2026-07-20 | RM-03 (Database Migrations & Persistence Layer) merged into `develop` (commit `2cc8a2e`) via a regular merge commit, following Principal Engineer review — approved with no blocking issues, including the required `CredentialEncryptionProvider` abstraction (Fernet as the initial implementation). Delivers ten SQLAlchemy models matching `DATABASE_SCHEMA.md` field-for-field, one thin repository per model, and one initial Alembic migration covering the full schema plus the `(camera_id, track_id)` active-incident dedup constraint. Verified against a live PostgreSQL container: reversibility across multiple up/down cycles, the dedup constraint, and every CHECK constraint. All gates re-verified clean on `develop` post-merge: 202 tests passing, 100% coverage. CI gained a `postgres:16-alpine` service container. `feature/api` retained as a long-lived subsystem branch. |
| 2026-07-20 | RM-06 (Threat Engine Service) merged into `develop` (commit `25c7f5b`) via a regular merge commit, following Principal Engineer review — approved with no blocking issues. Delivers `services/threat_engine`: `rules.py` (deterministic classify() covering every THREAT_ENGINE_SPEC.md table row), `engine.py` (per-track escalation/de-escalation state machine: 3-frame HIGH debounce, 1s/2s/3s incident/alarm timers, 10s/5s/3s de-escalation hysteresis, FIRE immediate bypass), `types.py` (`EscalationSignal` — see Snapshot's Design Note). 113 new tests (exhaustive rule table + precedence + determinism; engine timers/debounce/de-escalation/FIRE/HUMAN_REVIEW/track-isolation). All gates re-verified clean on `develop` post-merge: 176 tests passing, 100% coverage. `feature/threat-engine` retained as a long-lived subsystem branch. |
| 2026-07-20 | RM-DEV (Developer Infrastructure) merged into `develop` (commit `35e4f77`) via a regular merge commit, following Principal Engineer review — approved with no blocking issues. Delivers: ruff + black (required CI checks), mypy (advisory-only, `continue-on-error` in CI, 3 pre-existing findings recorded as technical debt for the owning subsystem to resolve when those files are next touched for functional work), pre-commit hooks (file hygiene + black + ruff; markdown docs excluded since they're under architecture-freeze change control), GitHub Actions CI scoped to PRs/pushes targeting `develop` only (not `feature/*`), pytest-cov coverage reporting with no minimum threshold enforced yet, and pinned dev-tool versions (ruff/black/mypy/pre-commit/pytest-cov/types-PyYAML) in `requirements-dev.txt`. Existing RM-01/RM-02 code brought to a clean black/ruff baseline (formatting-only). All gates re-verified clean on `develop` post-merge; 63 tests passing, 100% coverage on existing code. `feature/developer-infrastructure` retained as a long-lived subsystem branch. |
| 2026-07-20 | Branch hierarchy restructured to `main` (production) / `develop` (integration) per updated governance: `develop` fast-forwarded to absorb the prior `master` history, `feature/api` (RM-01) merged into `develop` (commit `cbcc49c`), 63 tests passing. `main` intentionally not created yet — reserved for the first full production release. `feature/developer-infrastructure` created as a new long-lived subsystem branch (formatting, linting, static analysis, dependency management, pre-commit, CI/CD, coverage tooling, developer workflow), split out from `feature/testing`, which now scopes exclusively to validation/regression/benchmarking/soak testing/evaluation. RM-DEV ownership moved to `feature/developer-infrastructure`. |
| 2026-07-19 | RM-02 (Shared Contracts Package) merged into `master` (commit `8a39b34`) via a regular merge commit, following Principal Engineer review. Two blocking issues were found and fixed pre-merge: `IncidentStatus` used `CLOSED` and omitted `ARCHIVED` (corrected to match `docs/INCIDENT_LIFECYCLE.md`'s five-state lifecycle: NEW/ACTIVE/ACKNOWLEDGED/RESOLVED/ARCHIVED); `ReviewStatus` used `PENDING` instead of `OPEN` (corrected to match `docs/DATABASE_SCHEMA.md`'s `human_review_items.status` values). `feature/shared-contracts` retained as a long-lived subsystem branch. |
| 2026-07-19 | RM-02 (Shared Contracts Package) implemented and tested on `feature/shared-contracts`: `shared/constants` (ThreatLevel, DistanceZone, WeaponType, UniformClass, IncidentType, IncidentStatus), `shared/events` (EventEnvelope + 10 typed event aliases matching EVENT_CONTRACTS.md), `shared/schemas` (ApiResponse + threat/incident/review/camera/alarm schemas from FRONTEND_BACKEND_CONTRACTS.md). `conftest.py` and `pytest.ini` added at repo root. 52 tests passing. |
| 2026-07-19 | RM-01 (Repository & Runtime Foundation) implemented, tested (11 passing tests), merged into `feature/api` |
| 2026-07-19 | Repository governance reconciled with actual workflow: `docs/IMPLEMENTATION_ROADMAP.md` created as single source of truth for milestone sequencing; this document, `CLAUDE.md`, and `PROJECT_CONTEXT.md` updated to match the subsystem-branch model |
| 2026-07-16 | Repository skeleton, package structure for `apps/`, `services/`, `shared/` |
| 2026-07-16 | Architecture freeze v2 — priority-ordered spec set in place |
| — | Frontend prototype UI (separate repo, pre-dates this architecture — requires audit) |

---

## Implementation Notes

Running log of build-time findings that future contributors (human or AI) need but that don't belong in an ADR or spec. Newest first. Keep entries short. Move anything that becomes a durable architecture decision into an ADR instead of leaving it here.

- 2026-07-23 — **`nvstreammux` never reaches PLAYING with fewer active sources than `batch-size` unless `batched-push-timeout` is set.** Discovered during RM-11.SIV's real-hardware verification: a standalone harness driving the real `DeepStreamPipeline` sat at `frames_processed=0` for 3+ minutes with zero bus errors and no obvious cause. Isolating pipeline construction alone (no async orchestration) and polling `pipeline.get_state(0)` directly showed it stuck at `PAUSED, pending=PLAYING` indefinitely. Setting `batched-push-timeout` on the already-built element at runtime (bypassing `builder.py` entirely, as a diagnostic) fixed it in under 5 seconds — confirming the missing property, not something else, as the cause. This had been present since RM-11 Phase 0 (`streammux_batch_size` defaults to 20) but apparently never manifested in RM-11 Phase 1/2's own hardware verifications — worth remembering that a working multi-frame pipeline run does not by itself prove every source-count/batch-size combination works; single-camera-vs-batch-size-20 is exactly the SIV case that finally exercised it. Fixed with `DeepStreamSettings.streammux_batched_push_timeout_us` (default 40000µs).
- 2026-07-23 — **A component fed by a derived/computed value (not a direct event hook) is a Unified-Heartbeat trap.** RM-11.SIV's `Watchdog` initially computed `pipeline_fps` liveness by tracking `PerformanceInstrumentation.snapshot().frames_processed` across successive `check_once()` calls, entirely inside `Watchdog`'s own instance state — never writing anything back to the shared `HeartbeatRegistry`. This passed every SDK-free unit test (the tests only ever asked `Watchdog` itself), but on real hardware the `Dashboard` (which only ever reads `HeartbeatRegistry` directly, correctly, per the design) showed `pipeline_fps` permanently stalled while `Watchdog.check_once()` simultaneously reported it healthy — two consumers of "the same" data, silently disagreeing. Fixed by finding the actual per-frame event this was trying to approximate (`RuntimeAdapter.on_frame_observation`, which already existed) and beating the registry directly from there instead. Lesson for any future "derive component X's liveness from existing numeric state" temptation in this codebase: if two things are supposed to be reading one source of truth, only one of them should ever be *computing* anything — the other must be a pure read.
- 2026-07-22 — **RM-11 reference-environment detection and verification**, performed by direct inspection (not assumption) per an explicit repository-environment-update instruction. Detected: DeepStream SDK 7.0.0 (`/opt/nvidia/deepstream/deepstream-7.0/version`), GStreamer 1.20.3 (`gst-inspect-1.0 --version`), CUDA 12.2 / driver 535.288.01 (`nvidia-smi`, `nvcc --version`), TensorRT 8.6.1 (`libnvinfer` 8.6.1.6 via dpkg), GPU NVIDIA GeForce RTX 3060 12GB, `nvstreammux`/`nvinfer` GStreamer plugins present (`gst-inspect-1.0`). Two Python interpreters exist on this machine: `/usr/bin/python3` (system, 3.10 — the one DeepStream 7.0/Ubuntu 22.04's `gi`/`Gst`/TensorRT-python bindings are actually installed against) and `/home/dev/miniconda3/bin/python3` (3.13 — what this repo's own `pytest`/`ruff`/`black`/`mypy` run under, per earlier RM-11 Phase 0 work). Neither had `pyds` (DeepStream's Python metadata bindings) installed; `gi`/`PyGObject` and TensorRT's Python module were present only under the system interpreter. Installed `pyds` 1.1.11 user-locally (`pip install --user`) under the system Python from NVIDIA-AI-IOT's `deepstream_python_apps` v1.1.11 release wheel (the last release with a `py3-none` x86_64 wheel; newer releases are `cp312`-only, which doesn't match this machine's system Python 3.10) — this repo has no dependency on `pyds` under its own `requirements*.txt`/test suite; it is a machine-level DeepStream SDK companion, not a repo Python dependency. Built and ran a throwaway smoke-test script (`/tmp/rm11_env_verify.py`, not committed — scratch, reproducible from this note) under the system Python: `uridecodebin` (a DeepStream sample video) → `nvstreammux` → `nvinfer` (DeepStream's own stock `resnet18_trafficcamnet` sample detector, deliberately **not** the project's own weapon/uniform models — RM-11 design review Decision C keeps model readiness untouched) → a pad probe parsing `NvDsBatchMeta` via `pyds` → the real, unmodified Phase 0 `AsyncBridge.schedule()` → the real `RuntimeAdapter.on_pipeline_error()` → the real `InProcessEventBus.publish()`, all imported directly from `apps/deepstream/app`/`shared/events`. Result: `nvstreammux` loaded, pipeline reached `PLAYING`, PGIE loaded and built/ran its TensorRT engine, 1442 frames processed with metadata on every one, 29019 objects detected total, every batch's metadata successfully reached `RuntimeAdapter` and was published as a `SystemEvent` observed by a real subscriber, zero errors/exceptions. First-run TensorRT INT8 engine build took several minutes under GPU contention (see next note) — not a hang. Also discovered, incidentally, while surveying this machine: `~/Downloads/DeepStream-Yolo/` contains an already-running `deepstream-app` benchmark process and pre-built TensorRT engines named `yolo26m_weapon.onnx_*.engine` / `vit_binary.onnx_*.engine` plus a `bench_results.json`/`benchmark_result_sheet.pdf` — apparently prior, out-of-repo work matching this project's model names. This was **not** touched, imported, evaluated, or relied upon for the verification above (which used the stock sample detector instead), and does not change `MODEL_REGISTRY.md`'s "no model is production approved until benchmarked" status — noted here only so whoever owns model approval knows it exists and where. This machine is now the reference development environment for RM-11 (per the same instruction) — but not a substitute for validating against the actual Jetson AGX Orin deployment target.
- 2026-07-22 — This same GPU was already under load (~99% utilization) from the pre-existing `~/Downloads/DeepStream-Yolo` benchmark process (see the entry above) while the RM-11 environment-verification smoke test ran, which is almost certainly why its first-run TensorRT engine build took several minutes instead of the usual tens of seconds. Not a defect in the verification or in DeepStream/TensorRT itself — if this reference machine is reused for Phase 1 benchmarking, check for other GPU-resident processes (`nvidia-smi`) before treating any throughput/latency number as clean.
- 2026-07-21 — RM-08/RM-09/RM-10 were merged directly into `develop` without a design review or recorded Principal Engineer review, breaking `CLAUDE.md`'s Standard Development Workflow that every prior milestone followed. A Repository Integration Audit found and corrected: a CI-breaking test defect (RM-08, `Camera(status="ACTIVE")` — not a valid `CameraConnectionStatus` member, violates `ck_cameras_status`); a `shared/` ownership violation (RM-09 added `shared/schemas/health.py` from `feature/api`); a REST-path mismatch against `FRONTEND_BACKEND_CONTRACTS.md` (RM-09 used an undocumented `/api/v1` prefix and omitted `GET /health/recording`); and an unreconciled integration-pattern choice (RM-10's `AlarmService` subscribed to `AlarmRequestedEvent`, an event nothing produces, instead of a direct call mirroring RM-07's approved pattern). See Recently Completed for the full audit trail and correction commits. Lesson for future sessions: verify `IMPLEMENTATION_STATUS.md`'s "Principal Engineer review approval" and "quality gates clean" claims against actual review records and a real test run before trusting them, per this file's own Source of Truth policy — don't take a status claim at face value when the stakes of being wrong are high.
- 2026-07-20 — Milestone Status's RM-01 and RM-02 rows read as "**Done**" with no branch qualifier, unlike every later row's "**Done** (merged into `develop`)" -- ambiguous, since it looked like they might not be in `develop`. Verified against source: both merge commits (`cbcc49c` for RM-01, `8a39b34` for RM-02, the latter originally merged into `master` before `develop` absorbed `master`'s history during the branch restructuring) are ancestors of `develop`, and `shared/` (RM-02's deliverable) is present there. Not a real gap -- corrected the wording for consistency.
- 2026-07-20 — pytest hit a hard collection error ("import file mismatch") once a second file literally named `test_service.py` appeared under `tests/` without an enclosing `__init__.py` chain (`tests/services/calibration/test_service.py` vs. `tests/services/incident_service/test_service.py`) -- pytest's default "prepend" import mode names modules by basename alone in that layout, so the two collided the moment both were collected in one run (each file passed fine on its own). Fixed by adding `addopts = --import-mode=importlib` to `pytest.ini`, which resolves modules by full path instead. Will recur for any future service that names its test file `test_service.py` (a very likely name) if this config regresses -- verify `pytest --cov` still collects and runs the full suite (not just individual files) after adding one.
- 2026-07-20 — Postgres's `func.now()` (used for `server_default` on `created_at` columns, `apps/api/app/models/base.py`) returns *transaction start time*, not per-statement time -- two inserts in the same uncommitted transaction get an identical `created_at`. This broke a test relying on "latest by `created_at` wins" ordering (RM-05's `get_latest_for_camera()`) until the test was fixed to `commit()` between the two inserts, simulating the separate requests/transactions real usage would have. Not an implementation bug -- watch for it in any future test that creates >1 row of the same append-only-history table without committing in between and then asserts on `created_at` ordering.
- 2026-07-20 — mypy hit a hard error ("Duplicate module named conftest") once a second file literally named `conftest.py` appeared under `tests/` without an enclosing `__init__.py` chain (`tests/services/incident_service/conftest.py` vs. root `conftest.py`) — unlike advisory findings, this stops mypy from analyzing anything at all. Fixed by adding `mypy_path`, `explicit_package_bases`, and `namespace_packages` to `pyproject.toml`'s `[tool.mypy]`, so module names are computed from full file paths. Will recur for any future service that adds its own test `conftest.py` if this config regresses — verify `mypy .` still runs clean (no hard errors, only the accepted advisory findings) after adding one.
- 2026-07-20 — Alembic's `autogenerate` does not emit ENUM type drops in `downgrade()` for Postgres `sa.Enum` columns (it drops the tables but leaves the `CREATE TYPE`-created types behind), which breaks re-`upgrade()` after a `downgrade()` with "type already exists". Fixed by adding explicit `sa.Enum(name=...).drop(op.get_bind(), checkfirst=True)` calls to `downgrade()` for `incident_type`, `threat_level`, and `incident_status`. Watch for this on any future migration that adds a new Postgres-native enum column.
- 2026-07-20 — `Mapped[datetime | None]` / `Mapped[datetime]` columns without an explicit `DateTime(timezone=True)` default to `TIMESTAMP WITHOUT TIME ZONE` in Postgres, which raises at insert time against timezone-aware Python datetimes ("can't subtract offset-naive and offset-aware datetimes"). All `apps/api/app/models/*.py` timestamp columns now specify `DateTime(timezone=True)` explicitly; apply the same when adding new timestamp columns later.
- 2026-07-20 — `services/__init__.py` was missing since the original bootstrap (only its subdirectories had `__init__.py`); added it during RM-06 since it's needed to import `services.threat_engine` as an explicit package. Applies to the whole `services/` tree, not just threat_engine.
- 2026-07-20 — mypy's advisory findings included 5 `isinstance(x, EventEnvelope[SomePayload])` notes ("Parameterized generics cannot be used with class or instance checks") from `tests/services/threat_engine/test_engine.py`. Static-analysis-only — Pydantic's generic models made `isinstance` work correctly at runtime — but on 2026-07-23 these were cleaned up (not just left as advisory debt): replaced with `type(x) is X`, matching the pattern this same file already used at one call site (`assert [type(e) for e in events] == [ThreatAssessmentEvent]`). Same runtime behavior, zero mypy findings. See the 2026-07-23 CI-fix changelog entry below for the full list of mypy findings resolved that day.
- 2026-07-20 — `requirements-dev.txt`'s dev/quality tools (ruff, black, mypy, pre-commit, types-PyYAML) are pinned to exact versions rather than `>=`, so CI, pre-commit, and local dev installs can't drift apart. `ruff`/`black` pins match the versions pinned in `.pre-commit-config.yaml` exactly. Runtime deps in `requirements.txt` and the RM-01-era `pytest`/`pytest-asyncio`/`httpx` entries remain `>=` — unchanged, out of scope for this update.
- 2026-07-20 — Pre-commit's generic file-hygiene hooks (trailing-whitespace, end-of-file-fixer) initially reformatted every architecture/governance markdown file repo-wide on first run. That was reverted, and `.pre-commit-config.yaml` now excludes all `*.md` files repo-wide (`exclude: '\.md$'`) — those docs are under architecture-freeze change control and must never be touched by generic formatting tooling.
- 2026-07-20 — `master` is superseded by `develop` as the active integration branch (`main` reserved for production releases; not created yet). Any doc, script, or CI config still referencing `master` as the branch to build against should be treated as stale and pointed at `develop` instead.
- 2026-07-19 — This document previously carried its own "Roadmap Progress" table (RM-01–RM-07) sourced from `docs/TASKS.md`'s `RE-xxx` ranges. That table used the same `RM-XX` IDs as the actual, approved roadmap to mean entirely different milestones (e.g. its RM-01 was "AI model optimization," not "Repository & Runtime Foundation," which is what was actually built). It has been replaced by a reference to `docs/IMPLEMENTATION_ROADMAP.md`. Do not recreate a second milestone definition in this file.
- 2026-07-19 — `feature/backend-foundation` and the original `feature/rm-01-repository-runtime-foundation` branch have been retired; RM-01's work now lives on `feature/api`.
- 2026-07-19 — Except for `apps/api/`, every `apps/*` and `services/*` package still contains only `__init__.py`. Treat any subsystem not listed as "In Progress" or later above as literally empty before starting work.

---

## Related Documents

- `docs/IMPLEMENTATION_ROADMAP.md` — authoritative milestone sequence, deliverables, acceptance criteria, dependencies
- `TASKS.md` (root) — ticket-level backlog; format under review, unchanged for now
- `ADR_INDEX.md` — accepted architecture decisions
- `PROJECT_CONTEXT.md` — static project facts (hardware, repos, stack) and the subsystem-branch model
- `CLAUDE.md` — repository operating manual, including "Git Branching & Merge Strategy"
- `AGENTS.md` — runtime pipeline agent roster and communication rules

Note: `docs/TASKS.md` (a different file from root `TASKS.md`) is a candidate for archival per an earlier documentation-consolidation review and is no longer treated as authoritative for milestone sequencing or subsystem branch assignment. That review has not yet been executed as a file change.

---

## Changelog

| Date | Change | By |
|---|---|---|
| 2026-07-29 | First manual validation failure (login 401, dev administrator missing) root-caused to sandbox PostgreSQL data not persisting across a recurring reset boundary, not a code defect. `run.py` now self-heals via a new `ensure_test_user()`, hardware-verified never to overwrite an existing password/role or duplicate the account. New `docs/VALIDATION_LOG.md`. Commit `cd72732`. | Claude |
| 2026-07-29 | Project consolidation: pushed 19 local-only `develop` commits to `origin/develop` (fast-forward, no conflicts) -- Camera Runtime v1 was fully present locally but had never been pushed to GitHub. Added `run.py`, a one-command launcher (prerequisite checks, migrations, ordered service startup with health waits, `--validation` flag, crash detection, clean shutdown), hardware-validated end-to-end. New doc `docs/OPERATOR_RUN_SHEET.md`. Recently Completed, Next Immediate Action updated. Commit `b32b1e9`. | Claude |
| 2026-07-29 | Production Validation Mode implemented: single config switch (`configs/validation.yaml`'s `production_validation_mode.enabled`) reusing existing frame trace/stage loggers/dashboard/watchdog, hardware-validated on/off with no measurable overhead. New docs `PRODUCTION_VALIDATION_MODE.md`, `PRODUCTION_VALIDATION_CHECKLIST.md`, `PRODUCTION_RUN_GUIDE.md`. Recently Completed, Next Immediate Action updated. Commit `7d31840`. | Claude |
| 2026-07-29 | Camera Runtime v1 wired into `apps.deepstream.app.main`/`runtime.py` (previously feature-complete but never constructed in production); added a temporary Desired State polling loop; found and permanently fixed a real segfault in `remove_source()` (dynamic `nvstreammux`/`nvstreamdemux` pad release, confirmed unsafe against NVIDIA's own reference implementation and Developer Forum guidance), hardware-validated with 15 stress cycles. New Repository Milestones entry, new Recently Completed entry, Next Immediate Action updated (production run's Run Guide is next). Commits `42b6761`, `018318d`. | Claude |
| 2026-07-29 | Camera Runtime v1 marked feature-complete: all 7 blueprint steps (Source Manager, Frame Distributor, Runtime Supervisor, Desired State Synchronization, Telemetry, AI Runtime reorganization, Media Publisher) accepted and merged to `develop`, each hardware-validated. New Repository Milestones entry, new Recently Completed entry (full per-step commit-hash record), Next Immediate Action rewritten (implementation phase closed, production hardening next — deliberately left unspecified pending that phase's own definition), Subsystem Status DeepStream pipeline row updated. | Claude |
| 2026-07-26 | Repository Consolidation (Step 2 of 2, frontend): RM-13 (`radar-eye-command`) `git subtree`-imported into `frontend/`, full history preserved, merged into `develop`. `docs/FRONTEND_CONTEXT.md`/`docs/FRONTEND_GAP_ANALYSIS.md` updated from their pre-RM-13 state. Repository Consolidation complete. | Claude |
| 2026-07-26 | Repository Consolidation (Step 2 of 2, backend): RM-12 API Service merged `feature/RM-12-api-service` → `feature/api` → `develop`, per the approved merge order. One conflict, this file only, resolved by combining both branches' entries. Quality gates re-verified clean post-merge. `develop` now has both the complete DeepStream pipeline and a working API layer. | Claude |
| 2026-07-26 | Repository Consolidation (Step 1 of 2, backend): `feature/deepstream` merged into `develop` (`3c84bcb`, regular merge commit) via the Repository Integration Audit's approved merge order, closing the RM-11.SIV Visualization addendum's last open gap. No conflicts (verified with a dry-run merge in a disposable worktree before executing). Post-merge quality gates re-verified clean: ruff clean, black clean, mypy clean, pytest 398 passed / 0 failed / 55 skipped (DB-dependent skips, expected baseline). Milestone Status (RM-11 row) and Subsystem Status (DeepStream pipeline row) updated to remove now-inaccurate "pending promotion" language. RM-12 merge (`feature/RM-12-api-service` → `feature/api` → `develop`) and the RM-13 frontend `git subtree` migration follow as the remaining Repository Consolidation steps. | Claude |
| 2026-07-25 | RM-12 (API Service) implemented on `feature/RM-12-api-service` -- all 6 phases (auth, audit, read-only REST, mutating REST, WebSocket bridge, contract verification) complete; `GET`/`PATCH /config` and `/ws/tracking` explicitly descoped and recorded in `docs/OPEN_QUESTIONS.md` (Q-014, Q-015) rather than guessed at; `docs/DATABASE_SCHEMA.md` corrected to add the `audit_log` table ADR-008 already mandated; `docs/FRONTEND_BACKEND_CONTRACTS.md`'s `PATCH /users` corrected to `PATCH /users/{user_id}`; Next Immediate Action, Milestone Status, Subsystem Status, and Recently Completed updated; not yet reviewed or merged into `develop` | Claude |
| 2026-07-25 | `[Cross-Subsystem] IncidentService` -- added `IncidentService.request_transition()` on `feature/RM-12-api-service`, an intentional, explicitly-approved touch to `services/incident_service/` (owned by `feature/incident-service`) needed for RM-12 Phase 4's `PATCH /incidents/{id}`; ownership of `services/incident_service/` is unchanged, `feature/incident-service` remains its owning branch, `IncidentService` remains the sole owner of lifecycle transitions; committed standalone with the `[Cross-Subsystem]` prefix; Recently Completed updated | Claude |
| 2026-07-24 | RM-11 closed: PR #8 (`feature/RM-11-SIV-visualization` → `feature/deepstream`) merged via regular merge commit `387a1ee` after a code review round (one real defect fixed — `VisualizationManager.mark_failed()` teardown, see Recently Completed); post-merge verification clean (tests, baseline tag, frozen docs/artifacts all confirmed intact); Milestone Status, Subsystem Status, Next Immediate Action updated; current milestone is now RM-12 (API Service), planning phase | Claude |
| 2026-07-23 | RM-11.SIV Visualization subsystem implemented and hardware-verified on ticket branch `feature/RM-11-SIV-visualization` (cut from `feature/deepstream`, not yet merged): new `apps/deepstream/app/visualization/` package (`VisualizationManager`/`VisualizationPipelineBuilder`/`DeepStreamOverlayRenderer`/`overlay.py`/`TrackAnnotationRegistry`/`RtspStreamServer`), optional `tee` fork after SGIE, RTSP video output with bounding boxes/track IDs/SGIE/threat/zone/distance/FPS/latency overlay; found and fixed a real hardware defect (RTSP keyframe timing, `iframeinterval`) during Phase 5 verification; new Design note (RM-11, SIV Visualization) row, Recently Completed, `docs/SIV_BASELINE.md` OFF-vs-ON comparison, `docs/DEEPSTREAM_PIPELINE_SPEC.md` Stage 5.5, and `docs/SIV_OPERATOR_RUNBOOK.md` step 6.5 all added | Claude |
| 2026-07-23 | CI green again on `feature/RM-11-SIV-runbook`: fixed all 21 mypy findings (narrowed `str \| None` before `in`; a dataclass replacing an under-typed dict in `check_rtsp.py`; an `ENUM_CLASSES: list[type[Enum]]` annotation fixing a `Never`-inference bug in `test_constants.py`; the 5 parameterized-generic `isinstance` findings switched to `type(x) is X`; explicit `ModelStageSettings` construction instead of raw dicts; a local `HealthCollectorLike` Protocol so `HeartbeatScheduler`'s test double satisfies its dependency structurally without an apps.api import; a scoped `type: ignore[call-arg]` for `EnvSettings()`'s known pydantic-settings false positive; a real bug where two DB queries shared a `result` variable, making mypy infer the wrong row type for the second; a `type: ignore[method-assign]` for an intentional test spy) and 2 runtime test failures (`_handle_escalation` was beating the `incident` heartbeat once per `EscalationSignal` instead of once per incident actually engaged, double-counting on FIRE's fast path where one detection yields both an `INCIDENT_ELIGIBLE` and `ALARM_ELIGIBLE` signal for the same incident; `Watchdog.check_once()` gained an injectable `now`, matching `HeartbeatRegistry.status()`'s existing pattern, so its falling-edge test forces staleness deterministically instead of racing real elapsed time against a threshold of `0.0`). Investigated the reported "database setup" log noise against every `db_engine`/`db_session` fixture and `configs/settings.yaml`; found no corresponding code defect (all consistently target the CI Postgres service's actual `radar_eye` database). Added `scripts/check_environment.py` (full environment pre-flight: GPU/driver/CUDA/TensorRT/DeepStream/GStreamer/PostgreSQL/Python version/required packages/env vars/model+engine+label files/disk space/writable output directories, one PASS/FAIL line per check) per Principal Engineer request, made it step 1 of `docs/SIV_OPERATOR_RUNBOOK.md` (renumbering the rest), and added an operator decision tree to the runbook's top. Merged `feature/RM-11-SIV-runbook` → `feature/deepstream` → `develop` (regular merge commits); quality gates re-verified clean after every merge (0 failed, ruff clean, black clean, mypy clean); all three branches pushed. | Claude |
| 2026-07-23 | RM-11.SIV merged into `develop` (`feature/RM-11-SIV` → `feature/deepstream` → `develop`, regular merge commits, per explicit Principal Engineer instruction); `docs/IMPLEMENTATION_STATUS.md` merge conflict (both branches had updated Next Immediate Action/Milestone Status/Subsystem Status/Recently Completed/Changelog independently) resolved by combining both branches' entries rather than discarding either; post-merge quality gates re-verified on `develop` | Claude |
| 2026-07-23 | DeepStream-Yolo real-model integration path reviewed (per Principal Engineer request) and wired into RM-11.SIV's config surface on `feature/RM-11-SIV`: vendored custom bbox-parser source (`apps/deepstream/native/`), extended `ModelStageSettings`/templates/`ModelConfigResolver`, fixed an `infer-dims` hardcoding defect found in the process; Design note (RM-11, SIV) follow-up, Blocking Issues note, Recently Completed updated; `docs/MODEL_REGISTRY.md` updated with confirmed compatibility facts (approval status unchanged) | Claude |
| 2026-07-23 | RM-11.SIV (System Integration Validation) implemented and hardware-verified on ticket branch `feature/RM-11-SIV` (cut from `feature/deepstream`); new Design note (RM-11, SIV) row, Next Immediate Action, Milestone Status, Subsystem Status, and Recently Completed updated; `docs/IMPLEMENTATION_ROADMAP.md` RM-11 addendum and Changelog also updated; new `docs/SIV_VALIDATION_REPORT.md` filled in with real hardware-run evidence | Claude |
| 2026-07-22 | Added a new "Repository Milestones" section recording the first fully green repository validation as a durable, standalone entry, per explicit request | Claude |
| 2026-07-22 | RM-08 async event-delivery test race fixed on `feature/recording` and merged into `develop` following Principal Engineer review approval (no production code changed); repository quality gates now fully green (278 passed, 0 failed, 49 skipped) for the first time; new Snapshot "Quality gates" row and Recently Completed updated | Claude |
| 2026-07-22 | RM-11 Phase 2 merged into `develop` following Principal Engineer review approval (one required follow-up, completed before merge); post-merge quality gates re-verified; Next Immediate Action (now Phase 3), Milestone Status, Subsystem Status, and Recently Completed updated | Claude |
| 2026-07-22 | RM-11 Phase 2's required latency-instrumentation follow-up completed on `feature/deepstream`: root-caused via a real-hardware PTS-based diagnostic, fixed (rolling-average latency, no application behavior change), re-verified on hardware; Design note (RM-11, Phase 2), Next Immediate Action, and Recently Completed updated | Claude |
| 2026-07-22 | RM-11 Phase 2 (SGIE, ThreatEngineRuntimeAdapter, full Calibration/Threat Engine/Incident/Alarm/Event orchestration) implemented on `feature/deepstream` and hardware-verified; new Design note (RM-11, Phase 2), Next Immediate Action, Milestone Status, Subsystem Status, and Recently Completed updated; not yet merged into `develop` | Claude |
| 2026-07-22 | `CalibrationService` in-memory homography cache (RM-11 Phase 2 prerequisite, Decision A) implemented on `feature/calibration` and merged into `develop`; Design note (RM-05) and Recently Completed updated; quality gates re-verified post-merge | Claude |
| 2026-07-22 | RM-11 Phase 1 merged into `develop` following Principal Engineer review approval (no blocking findings); post-merge quality gates re-verified; Next Immediate Action, Milestone Status, Subsystem Status, and Recently Completed updated; Phase 2 design review now authorized (implementation not authorized) | Claude |
| 2026-07-22 | RM-11 Phase 1 (PGIE, NvDCF tracker, performance instrumentation) implemented on `feature/deepstream` and verified end-to-end on physical hardware; new Design note (RM-11, Phase 1), Next Immediate Action, Milestone Status, and Subsystem Status updated; not yet merged into `develop` | Claude |
| 2026-07-22 | RM-11 architectural foundation (environment verification, Runtime Verification Review, ADR-027) merged into `develop` via a regular merge commit, per approval, independently of Phase 1; post-merge quality gates re-verified; Next Immediate Action status text and Recently Completed updated | Claude |
| 2026-07-22 | ADR-027 (DeepStream Runtime Adapter as Anti-Corruption Layer) added to `ADR_INDEX.md` — a cross-milestone architecture invariant, not an RM-11-scoped design note; Design note (RM-11, Runtime Verification) updated to point to it as the authoritative source; Recently Completed entry added. No code changes required (current code already complies). | Claude |
| 2026-07-22 | RM-11 Runtime Verification Review approved; new Design note (RM-11, Runtime Verification) row records the anti-corruption-layer constraint (Runtime Adapter isolates all `pyds`/`NvDs*` types from downstream services); Recently Completed entry added. No code changes required. | Claude |
| 2026-07-22 | RM-11 reference-environment detection and verification: confirmed by direct inspection (DeepStream 7.0.0, GStreamer 1.20.3, CUDA 12.2, TensorRT 8.6.1, RTX 3060), installed `pyds` 1.1.11 under the system Python, ran a minimal end-to-end pipeline smoke test (nvstreammux/PLAYING/PGIE/inference/pyds-parsed metadata reaching the real Phase 0 `AsyncBridge`/`RuntimeAdapter`/`InProcessEventBus`) — all passed, zero errors; new Reference environment (RM-11) Snapshot row and two Implementation Notes entries added; Next Immediate Action's blocking-issues text updated. No Phase 1 code written — scope stayed at environment verification per instruction. | Claude |
| 2026-07-22 | RM-11 (DeepStream AI Pipeline) Phase 0 merged into `develop` following Principal Engineer review approval (no blocking findings); post-merge quality gates re-verified; Design note (RM-11), Next Immediate Action (now Phase 1), Milestone Status, and Subsystem Status updated | Claude |
| 2026-07-21 | RM-11 (DeepStream AI Pipeline) Phase 0 implemented on `feature/deepstream` following an approved design review (runtime topology, calibration hot-path ownership, model-readiness deferral, 20-camera target, calibration acceptance criteria); synced the stale `feature/deepstream` branch with `develop` first; Snapshot (new Design note (RM-11)), Next Immediate Action, Milestone Status, and Subsystem Status updated. Not yet pushed/reviewed/merged. | Claude |
| 2026-07-21 | Repository Integration Audit: found RM-08/09/10 were merged into `develop` without design/PE review; reproduced the CI failure exactly (RM-08 test defect) rather than guessing; findings classified into Category A/B/C per PE instruction. Category A (RM-08 test fixture fix) and Category B (RM-09 ownership + route corrections, RM-10 integration boundary correction — each individually reviewed and approved) implemented and merged into `develop`. Category C: this file's Snapshot restored (RM-08's docs commit had deleted five Design Note rows and the branching-model rows), RM-09/RM-10 Design Notes added, Milestone/Subsystem Status corrected, and the RM-08/RM-09 "quality gates clean"/"Principal Engineer review approval" claims addressed via new Recently Completed entries rather than silently rewritten | Claude |
| 2026-07-20 | RM-05 (Calibration Service) merged into `develop` (`29e0261`) following Principal Engineer review approval; all quality gates re-verified clean post-merge; Snapshot, Next Immediate Action, Milestone Status, and Subsystem Status updated to reflect the merge | Claude |
| 2026-07-20 | RM-05 (Calibration Service) implemented on `feature/calibration` following an approved design review (append-only calibration history/"latest wins" convention; no reprojection-error acceptance gate); synced the stale `feature/calibration` branch with `develop` first; fixed a pytest module-collision issue affecting per-service `test_service.py` files; Snapshot, Next Immediate Action (now RM-08), Milestone Status, and Subsystem Status updated | Claude |
| 2026-07-20 | RM-07 (Incident Service) merged into `develop` (`90c21cc`) following Principal Engineer review approval; all quality gates re-verified clean post-merge; Snapshot, Next Immediate Action, Milestone Status, and Subsystem Status updated to reflect the merge | Claude |
| 2026-07-20 | RM-07 (Incident Service) implemented on `feature/incident-service` following an approved design review (escalation-logic ownership stays with Threat Engine; Threat Engine Runtime Adapter deferred to RM-11); fixed a mypy module-resolution issue affecting per-service test `conftest.py` files; Snapshot, Next Immediate Action (now RM-05), Milestone Status, and Subsystem Status updated | Claude |
| 2026-07-20 | RM-04 (Internal Event Bus) merged into `develop` (`832cc6e`) following Principal Engineer review approval; all quality gates re-verified clean post-merge; Snapshot, Next Immediate Action, Milestone Status, and Subsystem Status updated to reflect the merge | Claude |
| 2026-07-20 | RM-04 (Internal Event Bus) implemented on `feature/shared-contracts` following an approved design review (delivery/ordering/back-pressure/replay policy decisions); corrected stale "event bus" ownership wording in `PROJECT_CONTEXT.md`; Snapshot, Next Immediate Action (now RM-07), Milestone Status, and Subsystem Status updated | Claude |
| 2026-07-20 | RM-03 (Database Migrations & Persistence Layer) merged into `develop` (`2cc8a2e`) following Principal Engineer review approval; all quality gates re-verified clean post-merge; Snapshot, Next Immediate Action, Milestone Status, and Subsystem Status updated to reflect the merge | Claude |
| 2026-07-20 | RM-06 (Threat Engine Service) merged into `develop` (`25c7f5b`) following Principal Engineer review approval; all quality gates re-verified clean post-merge; Snapshot, Next Immediate Action, Milestone Status, and Subsystem Status updated to reflect the merge | Claude |
| 2026-07-20 | RM-06 (Threat Engine Service) implemented on `feature/threat-engine` following an approved design review (fire-precedence and AlarmRequestedEvent/incident_id design decisions); Snapshot, Next Immediate Action (now RM-03), Milestone Status, and Subsystem Status updated | Claude |
| 2026-07-20 | RM-DEV (Developer Infrastructure) merged into `develop` (`35e4f77`) following Principal Engineer review approval; all quality gates re-verified clean post-merge; Snapshot, Next Immediate Action, Milestone Status, and Subsystem Status updated to reflect the merge | Claude |
| 2026-07-20 | RM-DEV implemented on `feature/developer-infrastructure` (ruff, black, mypy-advisory, pre-commit, CI, coverage reporting); Snapshot, Next Immediate Action (now RM-06), Milestone Status, and Subsystem Status updated | Claude |
| 2026-07-20 | Branch hierarchy restructured to main/develop model: `develop` established as the active integration branch (absorbed prior `master` history), RM-01 merged into `develop`, `feature/developer-infrastructure` created and given RM-DEV ownership (split from `feature/testing`); Snapshot, Next Immediate Action, Milestone Status, and Subsystem Status updated accordingly | Claude |
| 2026-07-19 | RM-02 merged into master (`8a39b34`); Next Immediate Action set to RM-DEV; Snapshot, Subsystem Status, and Recently Completed updated to reflect the merge and the pre-merge review fixes | Claude |
| 2026-07-19 | RM-02 complete: updated Snapshot, Next Immediate Action, Milestone Status, Subsystem Status, Recently Completed | Antigravity |
| 2026-07-18 | Initial version created | Claude |
| 2026-07-18 | Added Roadmap Progress, Next Immediate Action, and Architecture Contract sections; reordered recommended read sequence to place `TASKS.md` before this file | Claude |
| 2026-07-18 | Re-sourced Roadmap Progress from `TASKS.md` §1.1/§2.3 instead of `IMPLEMENTATION_PLAN.md`; added Source of Truth note | Claude |
| 2026-07-19 | Full reconciliation: removed the conflicting Roadmap Progress table (superseded by new `docs/IMPLEMENTATION_ROADMAP.md`); corrected Subsystem Status branches to the actual long-lived subsystem branches (`feature/api`, `feature/deepstream`, `feature/threat-engine`, `feature/incident-service`, `feature/recording`, `feature/calibration`, `feature/shared-contracts`, `feature/frontend-integration`, `feature/deployment`, `feature/testing`); removed the unratified `@agent-*` Owner column; corrected Next Immediate Action to RM-02; corrected Snapshot to reflect RM-01's completion; removed `develop` as a stated integration branch | Claude |
